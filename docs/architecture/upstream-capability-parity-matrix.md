# 上游完整能力对照矩阵

**状态：** Phase 02 基线；FlClash 已记录 2026-09-06 真机 capability-parity 结果，其余固定提交待逐项确认

**适用范围：** `PRO-*`、`CLN-*`、`MED-*`、`IMG-*`

## 使用规则

这份矩阵是四个固定上游提交的完整能力基线。`XToolpro 映射`描述能力应位于哪个 `engine-*` 合同后；它不允许把上游能力改写成相似的自研版本。`Verified` 表示该合并行内的能力均有真实上游行为证据；`Partial` 表示只验证了其中一部分或仅确认真机入口；`Pending` 表示仍未形成足够真机证据。每一行必须有真实上游调用、依赖/许可证记录和对应测试证据后，才能将状态改为 `Verified`。

2026-09-09 Phase 02 gate 只读汇总：按本文件状态列重新统计为 `Verified=3`、`Partial=61`、`Pending=29`、`Unavailable=1`、`Blocked=0`。`Partial`/`Pending` 不是已批准能力；与四域台账仍均为 `Investigating`、五类 engine 结果尚只有测试计划而无可执行 adapter 证据一起，明确阻止 Phase 02 完成和任何正式 `engine-*` 集成。

允许替换的内容只有 XToolpro 品牌、图标、翻译、统一导航、任务/通知壳和 Android 平台适配。上游明确排除的品牌材料、未声明许可的组件、设备不支持的能力或合规禁止的绕过流程，必须记录为 `Blocked` 或 `Unavailable`，不得静默删除。

## FlClash：代理能力

固定提交：`62addf738a76b1a492e19af2dbabdb6d572b9e72`
引擎：`engine-proxy`
来源记录：[ADR-0002](ADR/ADR-0002-flclash-proxy-engine-investigation.md)、[依赖盘点](../compliance/upstream-dependency-inventory.md)

| 上游能力 | 来源路径/组件 | XToolpro 映射 | 状态 |
| --- | --- | --- | --- |
| YAML、剪贴板、URL/订阅导入与导出 | `lib/views/profiles/add.dart`、`lib/widgets/input.dart`、`lib/providers/actions/profiles.dart`、`lib/common/link.dart`、`lib/common/request.dart`、`android/`、`core/` | 配置导入、校验、备份恢复、错误回退 | Partial：真机识别文件配置；确认二维码、文件、URL 添加入口；配置导出经 SAF 提交，系统文件选择器中配置卡片标签与导出文件名精确匹配 1 项，未打开或读取文件；本地备份经 SAF 保存后应用报告成功，系统 ZIP 选择器可见对应文件；独立配置剪贴板导入在固定 Android 包中为 Unavailable，源码和真机添加页均只有二维码、文件、URL 三项，URL 文本框长按未显示“粘贴”动作且未改写剪贴板，标准文本粘贴仍未验证。固定 Android manifest 未声明标准 `ACTION_SEND`，真机 package-restricted `ACTION_SEND` 文本分派也在启动前被 Android 拒绝；该入口为 Unavailable。固定包声明 `clash`、`clashmeta`、`flclash` 的 `install-config` 深链接；真机以公开保留域名分别在应用已初始化和强停后的冷启动状态触发 `flclash`，均显示一次导入确认并返回取消；对 `clash`、`clashmeta` 的 package-restricted `ACTION_VIEW` 也各显示一次确认并取消，均未写入或联网。未读取全局默认处理者，故不外推系统 resolver 选择。该入口将 URL 置于 deep-link query，不是系统分享文本入口；未验证备份恢复。源码隐私审计：`LinkManager` 将完整 URI（含 `url` query）传给 `commonPrint.log`；app attach 后该未脱敏 payload 同时进入 `debugPrint` 和最多 500 项的内存 `logsProvider`，并可在日志页作为可选择文本显示。日志页的显式导出会把每条 `Log.toString()` 未脱敏拼接后经临时文件交给系统文件选择器；临时文件随后删除。`openLogs` 只控制 core log capture 与日志入口显示，不拦截 `commonPrint` 的写入。`getFileResponseForUrl()` 使用 `_clashDio` 直接按用户 URL 请求 bytes；固定 `Request()` 未设置 profile 下载的显式 connect/receive/send timeout、响应字节上限、Content-Type/大小预检、来源绑定或重定向策略，响应正文可先完整进入内存再交给配置校验。下载失败会把原始 `DioException.toString()` 写入日志。固定 Android `FilesProvider` 为受 `MANAGE_DOCUMENTS` 限制的导出 `DocumentsProvider`，但把整个 app `filesDir` 作为根目录，列出子项并允许按请求模式打开；canonical path 检查阻止越出根目录，普通文件仍标记为可写。未枚举设备文件或验证系统 URI 授权；XToolpro 不得用该整目录 provider 代替显式、最小化的 SAF 导出。静态源码补充：`Profile.saveFile()` 将输入写入 `appPath.tempFilePath` 后调用 `coreController.validateConfig()`，校验失败或协程取消没有 `finally` 清理；通过校验后以普通 `File.copy()` 覆盖 profile 文件，未见同目录临时发布、fsync、read-back 或失败回滚。`saveFileWithPath()` 同样在校验后直接覆盖目标。静态源码不足以断言任一运行时异常必含 URL，且本轮未发送真实订阅 URL。XToolpro 在任何 engine adapter/诊断导出获批前必须在边界处把失败原因归一为稳定、脱敏的类别或代码，禁止未脱敏订阅 URL 或异常文本进入 UI、应用/系统日志、导出或遥测；当前不构成正式集成批准 |
| 订阅更新、重命名、复制、删除和持久化 | `lib/views/profiles/profiles.dart`、`lib/views/profiles/edit.dart`、`lib/providers/actions/profiles.dart`、`lib/application.dart`、配置服务 | 配置仓库和版本化变更 | Partial：真机确认编辑、预览、覆写和删除入口；后续当前配置出现仅限 URL 类型的“同步”和“复制链接”，编辑页存在 3 个非空输入框、自动更新项和保存动作，未读取字段值或提交变更。静态路径显示“同步”最终调用 URL profile update，“复制链接”会写剪贴板而非克隆 profile；未发现用户可见复制/克隆 action。更新失败会把原始异常送入日志/UI，未见统一脱敏错误码；删除、重命名、URL 变更后的配置写入及文件清理没有可核验的原子提交、回滚或 terminal receipt。补充静态审计：`application.dart:86-90` 仅在 Flutter application 已 attach 后以固定 20 分钟 one-shot `Timer` 调用 `autoUpdateProfiles()`，完成后才递归安排下一次；没有 WorkManager/Alarm、持久化调度、网络约束、跨进程恢复或 timer callback 的 `finally` 重排程，进程死亡、detach 或未捕获异常都可能停止后续更新。`ProfilesAction.autoUpdateProfiles()` 顺序遍历当前内存 profile 快照，仅按 `autoUpdate`、`lastUpdateDate + autoUpdateDuration` 和 URL 类型筛选；每项调用 `updateProfile()` 无取消令牌、超时、并发去重或逐项 terminal receipt，异常原文直接写入 warning log。`updateProfile()` 先将旧 profile 放回 provider，再网络拉取并 `saveFile()`；文件提交成功与 provider/数据库更新之间没有事务或恢复标记，进程中断可能留下文件与元数据不一致。未执行订阅更新、重命名、删除、剪贴板写入或持久化行为验证，保持 `Partial` |
| Profile script 覆写与配置生成 | `lib/providers/actions/setup.dart`、`lib/common/javascript.dart`、`lib/models/common.dart`、`lib/providers/state.dart`、`flutter_js ^0.8.7` | 脚本覆写、配置预检和可回滚发布 | Partial（静态）：固定 `setup.dart:289-303` 在 `OverwriteType.script` 下读取完整脚本文本，并调用 `handleEvaluate()`；`javascript.dart:6-27` 先把 raw config 编码为 JSON，再将脚本与 `main(<完整配置>)` 拼接交给 `getJavascriptRuntime().evaluateAsync()`。未见超时、取消、runtime dispose、资源配额、网络/文件/环境访问沙箱或输入大小限制；`res.isError` 直接抛出 runtime 的 `stringResult`，结果以 `Map<String,dynamic>.from` 强制转换，未见输出 schema、版本/hash 或敏感字段约束。脚本文件由 `Script.content` 整体读入，`Script.save()` 普通 `writeAsString()` 覆盖；`saveWithPath()` 固定实现把源文件复制到自身路径，疑似无法导入；两条路径均未见原子写入、read-back、版本绑定或失败回滚。`getProfileWithId()` 将异常 `toString()` 直接交给 notifier；脚本执行后继续生成 YAML 并写入配置文件，不能形成独立、可等待、脱敏的 terminal receipt。未执行脚本、未读取设备文件或配置，保持 `Partial` |
| 规则/全局/直连模式 | `core/Clash.Meta`、配置路径 | 模式选择与状态持久化 | Verified：真机确认三种模式；全局和直连均经强停重启保持；直连模式下公开 HTTPS 返回 `200` 且 TUN 收发计数增长，结束时恢复规则模式并再次强停重启确认 |
| 代理组展开、节点切换和当前节点 | `core/`、Android bridge | 代理组/节点页面与运行状态 | Partial：规则模式下真机显示多个代理组、节点卡，以及“自动选择”“故障转移”组入口；2026-09-07 使用正确的 dev 包 launcher Activity 重新进入代理页，ADB UI hierarchy 对“自动选择”和“故障转移”各计数 1，未读取节点名或原始卡片文本；随后强停并重新启动同一 Activity，再次对两项各计数 1，证明入口在进程重建后仍可达。进入代理组详情后，ADB 仅统计到 6 个协议卡片，未读取卡片名称、流量额度或其他原始字段，也未点击任何卡片；随后仅在两个组标签之间切换，两个标签各自均渲染 6 个协议卡片，最后恢复原标签。固定源码中 `ProfilesAction.updateCurrentSelectedMap` 通过 `profilesProvider.put` 更新当前 Profile 的 `selectedMap`，`ProxiesAction.updateCurrentGroupName` 同样通过 `profilesProvider.put` 更新 `currentGroupName`；Profile DAO 将其持久化到 `selected_map`/`current_group_name` 字段，启动时再由 profile provider 读取。节点卡 `onPressed` 先更新 `selectedMap`，再经 debounce 调用 core `changeProxy`；固定路径未见在 core 失败时回滚已持久化的 Profile 映射或向 UI 提供稳定错误类别，因此存在 UI/持久状态与 core 实际选择短暂不一致的风险。设备内只读查询确认当前 Profile 的映射非空、当前组选择非空，强停重启前后条目数与规范化 SHA-256 完全一致，且未输出组名、节点名或原始值；直连模式按上游逻辑隐藏代理入口，恢复规则模式后入口重新出现；无敏感 UI 语义仍不能独立标识所选卡片，且未覆盖多个组/节点切换。proxy-provider 静态审计补充：`adapter/provider/provider.go` 使用带取消 context 的 `resource.Fetcher`，解析成功后才替换 `proxies`，并以 RWMutex 维护版本计数；HTTP response header 的 `subscription-userinfo` 会写入缓存并解析为 `SubscriptionInfo`。但 `providerForApi` 序列化完整 `C.Proxy` 列表、`testUrl`、expected status 与 subscription info，管理 API 的 `GET /providers/proxies`、单 provider `GET`、`PUT` 更新和 healthcheck 路由直接暴露这些对象/状态；更新失败以 `503` 返回原始 `err.Error()`，healthcheck 只返回无正文成功，不提供逐节点成功/取消/超时回执。`HealthCheck` 使用 provider 级 singleflight、最多 10 个并发探测和每代理 timeout，但日志包含 proxy name、URL、delay 与 alive 状态；固定路径未见字段最小化或脱敏策略。保持 `Partial` |
| 节点协议、地区、标签筛选 | `core/`、Clash.Meta provider | 筛选、排序和可用性标记 | Partial：固定 Clash.Meta provider 源码存在 `filter`/`exclude-filter` 正则字段；真机代理页未出现独立协议、地区或标签筛选控件，当前仅能确认上游内部字段，未验证用户可见筛选行为。静态补充：`NewProxiesParser` 按反引号分隔多个 `filter`/`exclude-filter` 正则，按 `exclude-type` 不区分大小写排除协议类型，再按代理 `name` 应用排除与包含规则；重复名称去重，空结果分别返回 filter 不匹配或无代理错误，正则语法错误和单项解析/override 错误会使整个 provider parse 失败。固定路径未建立协议/地区/标签字段的用户级模型、筛选结果数量/原因、空态与重试回执；未执行恶意正则、空结果、重复名或真实 provider fixture 验证，保持 `Partial` |
| 单节点与批量测速 | `core/`、服务层 | 测速任务、结果和失败原因 | Verified：单节点返回 `103 ms`；当前组批量测速显示 10 个结果节点和 9 个不同延迟值 |
| Android VPN 启停与竞争 VPN 处理 | `android/service`、`android/core`、`android/service/.../NotificationModule.kt`、`lib/common/preferences.dart`、`lib/views/dashboard/widgets/start_button.dart`、`lib/providers/actions/common.dart`、`lib/providers/actions/setup.dart` | `VpnService` 生命周期和权限流程 | Partial：真机多次完成启动、停止和恢复；规则模式下应用退到后台后，系统 VPN 与 `tun0` 归属仍存在，公开 HTTPS 返回 `204` 后仍保持连接，回到应用可正常停止并清理。运行态强停应用后 VPN/TUN 均被清理，重新启动后不自动残留连接；系统通知服务中与 FlClash 匹配的记录计数在 VPN 停止后从 13 降至 5，未读取通知正文。后续停止态中，经固定 `StartButton.handleSwitchStart()` 对应的精确仪表盘运行控制触发启动，20 秒内 `tun0` 始终不存在；再以同一控件恢复后仍保持无 TUN、FlClash 进程数为 1。最新一轮中，用户报告在已授权通知权限的主界面点击启动；0 秒与 7 秒只读回执均为 `tun0=0`、dev 进程存在、`POST_NOTIFICATIONS granted=true`、UID app-op `allow`；同一窗口的系统 VPN 连接标记计数为 1，但没有 `tun0`。后续仅统计所有 TUN 类型接口，结果为 0，排除“接口仅非 `tun0` 命名”的解释；故系统标记同样不足以作为 core/TUN 健康回执。固定 Dart 路径显示该按钮经 `toggleRunning()` 先调用 `_setLocalRunning(true)`，已初始化时只 await listener/service 的 start 请求，再无等待地 debounce `applyProfile()`；后者才会进入 Android setup，且该路径丢弃 `startListener()` 的布尔结果。Android `ServicePlugin.start()` 又只提交 `ServiceState.requestStart()` 并立即返回 true，`VpnService` 只有 `Builder.establish()` 取得 descriptor 后才标记 tunRunning 并调用 `Core.startTun()`。`establish()?.detachFd()` 发生在 native 调用前，且 `handleStart`/`start` 只捕获 `Exception`；`LinkageError` 等 Error 会绕过 service 内 cleanup。controller 以 `runCatching` 感知后虽尝试 stop，却仍依赖同一 `Core.stopTun` 路径，未给出稳定错误类别或 TUN cleanup 回执；不能据此断言实际 FD 泄漏，但这条错误路径没有隔离真机契约。故 UI、listener/service start request、profile setup、system establish 与 TUN 均非同一个完成回执。`prepareVpn()` 只在其 coroutine 被取消时才调用 `AppPlugin.cancelVpnPreparation`，而 `requestStop()` 仅替换 run token、不取消该 coroutine；所以 STOP 若发生在系统 VPN consent UI 等待期间，同样依赖系统结果/detach 才会使原 start Deferred 结束，未提供有界 `Cancelled` completion。竞争 VPN 仅由 Android 系统 `VpnService.prepare()`/`Builder.establish()` 隐式处理，固定 bridge 未见 active VPN 查询、冲突仲裁、明确 `Unavailable` 映射或保留原 VPN 的策略；若 establish 被系统拒绝，错误仍沿普通异常/布尔路径传播。未读取 UI 错误、配置或系统 VPN 正文，也未发起流量；该次结果不否定早前成功 proof，但说明 UI 请求不构成可复现的实际 VPN/TUN 启动回执。源码隐私审计：Flutter `currentProfile.label` 被传为 `NotificationParams.title`，前台服务更新时将其写入 Android 系统通知标题；初始中性 `FlClash` 标题随后会被覆盖，正文仅来自速度/流量文本，停止按钮来自本地化文案。Flutter 还将完整 Android 启动 `SharedState` 作为 JSON 写入 SharedPreferences，供原生读取；字段含测试 URL、配置标签、代理组选择映射、按应用路由包名列表和绕过域名，未见该路径的应用层加密。未读取设备通知或偏好内容，且不外推具体当前值；XToolpro 必须使用中性固定标题，且将 native 启动状态最小化、加密并禁止持久化订阅 URL、节点/组名或应用选择。未验证竞争 VPN、首次授权拒绝或撤销 |
| 配置应用失败、启动状态一致性与可恢复错误 | `lib/providers/actions/setup.dart`、`lib/core/controller.dart`、`lib/state.dart`、`lib/core/lib.dart`、`android/app/.../plugins/ServicePlugin.kt`、`ServiceState.kt`、`ServiceController.kt`、`android/service/.../VpnService.kt`、`android/core/.../Core.kt`、`android/core/.../core.cpp`、`core/lib.go`、`core/hub.go` | 配置应用、启动回执和稳定错误状态 | Partial（静态）：固定 `_setupConfig()` 将生成/写入后的 `setupConfig()` 包在 `loadingRun(silence: true)` 内；`safeRun()` 捕获异常、直接以 `e.toString()` 通知并返回 `null`，而 `_setupConfig()` 随后仍返回 `completed`。启动初始化时，`CoreController.setupConfig()` 与 `_setCoreRunning()` 并行 `.wait`，但后者的 `startListener()` 布尔结果已在 `SetupAction` 中被丢弃；普通启动则在该结果后不 await 地排队 `applyProfile()`。native `quickSetup` 还会先置 `isRunning=true` 再解析/应用配置，失败路径未在此处复位；随后 `startTUN` 据该标记决定仅 reset connections 还是启动 listener，故没有强制的 config-before-listener 顺序。`VpnService` 又会在调用 `Core.startTun()` 前置 `tunRunning=true`，而 Kotlin/JNI bridge 将该调用声明为 `Unit`/`void`，忽略 Go 层无条件 `true` 的返回；没有单独的 native health API，`tunRunning` 也未对外暴露。流量方法用于 UI/通知展示，fallback 同样可返回 `{}`，不能作为完整 core、listener 或 TUN 已健康的回执。配置应用错误、listener/service 请求失败及实际 TUN 建立不形成统一、可 await 的启动结果，不能由本地运行计时、流量对象或 MethodChannel `true` 推断成功。下层 `VpnService.start()` 虽会在 `Builder.establish()` 被拒绝或 JNI 调用抛错时同步失败，`ServiceController.start()` 也会归零 runtime，但 `ServicePlugin.start()` 不向 Dart 回传该 deferred 结果。固定源码仅证明错误有机会以原始异常文本进入通知/日志，不证明任何设备上的具体错误或泄露。XToolpro 必须以稳定、脱敏的错误码和显式状态机连接配置写入、core setup、VPN permission、`establish()`、TUN 核验及可转发性；失败、取消和超时均须停止本地“连接中/已连接”状态并可安全重试，完成真机契约测试前保持 `Partial` |
| 变体原生 START、强停与无意恢复 | `android/common/.../Enums.kt`、`Ext.kt`、`QuickActionActivity.kt`、`ServiceState.kt`、`VpnService.kt` | 显式启动、进程死亡清理与重开恢复 | Partial：小米 10S（API 33）停止基线下，解析安装的 dev 变体 action/component 后，`com.follow.clash.dev.action.START` 在 7 秒和 14 秒均形成 `tun0`，且 dev 进程存在；随后 `am force-stop com.follow.clash.dev` 后 5 秒，`tun0` 和 dev 进程均不存在。再以已解析的 dev `MainActivity` 打开应用，8 秒后进程存在但 `tun0` 未自动恢复。追加冷启动复核中强停后 3 秒进程/VPN/TUN 均为 0；launcher 启动后 8 秒仅进程恢复，`vpn_connected_markers=0`、`all_tun_interfaces=0`，最终再次强停后仍为 0，未观察到无新请求的意外恢复。另在运行态发出 `am kill com.follow.clash.dev`，第 5 秒和第 10 秒 dev 进程及 `tun0` 都仍存在，说明该命令在本设备没有诱发可观察的前台进程死亡，不能替代 crash/process-death proof；随后显式 STOP 在第 5、10 秒清理 TUN。另一次通知权限保持允许、强停后直接发送同一 START action，7 秒和14 秒均未形成 `tun0`，但 dev 进程存在；随后 STOP 保持无 TUN，说明获准权限本身不能作为可复现 TUN 启动回执。最新一轮在停止基线 `vpn_connected_markers=0`、`tun0=0`、dev 进程 `1`、`POST_NOTIFICATION=allow` 下发送同一 START，7 秒后为 `vpn_connected_markers=1`、`tun0=1`，进程/app-op 不变；紧接 STOP 后 7 秒回到 `vpn_connected_markers=0`、`tun0=0`、进程仍为 `1`。追加的受限复核在同一停止基线发送 START 后 7 秒观察到 `vpn_connected_markers=1` 但 `all_tun_interfaces=0`，随后 STOP 7 秒回到 `vpn_connected_markers=0`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变；这再次证明系统 VPN 标记不能替代实际 TUN/core 健康回执。该配对证明此轮 action 可建立并清理 TUN，但不把系统标记、进程或 app-op 当作 core health。固定源码由 `applicationId` 生成 QuickAction action，故必须以实际变体包名定位，不得把另一已安装包、旧 action 或系统 VPN 标记当成此结果。该 proof 仅覆盖显式 action 启动后的 force-stop 清理、`am kill` 未触发的边界及无新请求重开，不证明 core health、流量转发、reboot/always-on、竞争 VPN、通知权限撤销或 service callback 恢复；未发送流量、未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 变体原生 STOP 与实际 TUN 清理 | `android/common/.../Enums.kt`、`Ext.kt`、`QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt` | 显式停止、实际 TUN 清理和 UI 进程存活 | Partial：同一正确 dev action/component 路径中，START 后第 7 秒 `tun0` 与 dev 进程均存在；随后 `com.follow.clash.dev.action.STOP` 在第 5 秒及第 10 秒均使 `tun0` 不存在，而 dev 进程仍存在，证明该轮原生显式 STOP 可实际清理 TUN 而不结束 UI 进程。最新一次在用户授权的受限 action 检查中，STOP 前为 `vpn_connected_markers=1`、`all_tun_interfaces=0`、dev 进程 `1`、通知权限/app-op 为允许；STOP 后 7 秒为 `vpn_connected_markers=0`、`all_tun_interfaces=0`、dev 进程仍为 `1`、权限/app-op 不变。此轮仅证明 STOP 使系统 VPN 标记收敛，因前态已无任何 TUN，不能作为实际 TUN 清理的正向 proof。该结果不把 `ServicePlugin.stop()` 的立即 MethodChannel `true` 解释为同步完成，也不证明 core health、通知撤销、流量缓存清理、service destroy completion、异常/超时、reboot 或竞争 VPN 行为；未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 变体 TOGGLE action（快捷方式/磁贴共用路径） | `android/common/.../Enums.kt`、`Ext.kt`、`QuickActionActivity.kt`、`TileService.kt`、`AppPlugin.kt`、`ServiceState.kt` | 快捷启动/停止的可核验状态切换 | Partial：小米 10S 停止态以正确 dev `TOGGLE` action 启动后第 7 秒 `tun0` 存在、dev 进程存在；在运行态对同一 action 再次触发后，第 5 秒和第 10 秒 `tun0` 均不存在、dev 进程仍存在。另在正常临时撤销 API 33 `POST_NOTIFICATIONS` 后，强停并触发同一 action，第 7 秒和第 14 秒均未建立 TUN、进程存在；同次 STOP 后无 TUN，且权限已恢复为允许。固定源码显示动态快捷方式和 `TileService` 均分派到该 action，故该结果证明其共享原生入口可在获准态形成/清理 TUN、在本次撤销态观察中未形成 TUN；它不证明桌面快捷方式或快捷设置磁贴自身的系统 UI、通知可见性、permission callback、稳定 unavailable/cancel、core health、流量、异常/超时、reboot 或竞争 VPN。未发送流量，未读取系统 VPN/通知正文/日志/配置内容，保持 `Partial` |
| 冷启动原生 fallback 的 START/STOP | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt`、`AppPlugin.kt` | 无 Flutter 主界面预附着时的原生启动与停止 | Partial：强停 dev 变体后，`tun0` 与 dev 进程均不存在；不先打开 `MainActivity` 而直接发出正确 dev START action，在第 7 秒和第 14 秒均形成 `tun0`。随后正确 STOP action 后第 5 秒 `tun0` 不存在、dev 进程存在。该轮覆盖 `flutterEngine` 未预先由主界面附着时的原生 fallback 能建立及清理 TUN，但不证明 Flutter/Tile listener 已附着分支、core health、流量、通知权限、reboot/always-on、竞争 VPN、异常/超时或持久恢复；未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 主界面已启动后的原生 START/STOP | `MainActivity.kt`、`QuickActionActivity.kt`、`ServiceState.kt`、`TilePlugin.kt`、`AppPlugin.kt` | 前台 app 生命周期下的原生 action 边界 | Partial：先打开正确 dev `MainActivity` 并等待 8 秒，停止基线仍无 `tun0`；随后相同 dev START action 在第 7 秒和第 14 秒均建立 `tun0`，同一 STOP action 后第 5 秒清理。另一次通知权限允许、强停后打开主界面 8 秒，随后同一 START 在第 7 秒和第 14 秒均未建立 TUN、进程存在；重复 STOP 后仍无 TUN。固定源码显示 `MainActivity` 配置 Flutter engine 时附着 App/Service/Tile plugins，但各轮仅检查 TUN/进程/permission，不读取内部 plugin、Dart listener 或 UI 状态，故不能断言 action 实际走了哪个分支或解释两轮不一致。该 proof 不覆盖 core health、流量、通知权限、异常/超时、reboot、竞争 VPN 或 permission revoke；未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 快速 START→STOP 取消收敛 | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt`、`android/core/.../core.cpp`、`core/lib.go` | 取消、序列化和最终实际停止 | Partial：强停后的停止基线中连续发出正确 dev START 与 STOP；5 秒后 `tun0` 仍存在、dev 进程存在，说明已提交 STOP 不保证成为此窗口的最终 TUN 状态。追加复核中立即发送 START→STOP 后 7 秒和 14 秒均为 `vpn_connected_markers=1`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变；再次发送 STOP 后 5 秒才回到 `vpn_connected_markers=0`、`all_tun_interfaces=0`。随后单独再发 STOP，5 秒及 10 秒后 `tun0` 均不存在、dev 进程存在，恢复停止基线。固定源码中 QuickAction 以独立 `GlobalState.launch` 协程提交两种 action；`handleStartAction()` 在 `setupCore()` 后才调用 `requestStart()`，而 `handleStopAction()` 在尚未有 running request 时立即返回。因此该路径与实测结果相容，但未读取内部时序，不能认定为唯一根因。进一步确认冷启动 native fallback 的 `loadPreferencesAndStart()` 会先 await `setupCore()`，其中 `quickSetup()` 只等待 native callback、没有 timeout 或 cancellation handler；该阶段尚未调用 `requestStart()`，故 STOP 仅观察到 `running=false` 而不能取消 setup coroutine，engine detach 也只清空引用。JNI/Go 还会为该 callback 持有 global reference，直至 setup goroutine 返回；公开 bridge 没有取消 setup 或提前释放 callback 的入口。XToolpro 必须在命令入口以单一串行状态机记录取消意图，令 STOP 覆盖所有未完成的 setup/start 阶段，并以实际 TUN/health 停止回执完成。未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 快速 STOP→START 启动收敛 | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt` | 相反命令序列的最终实际运行 | Partial：停止基线下连续发出正确 dev STOP 与 START；第 5 秒和第 10 秒 `tun0` 均存在、dev 进程存在，表明本次序列最终收敛到运行态。追加复核在停止基线立即发送 STOP→START，7 秒和 14 秒均为 `vpn_connected_markers=1`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变；随后 STOP 5 秒后回到 `vpn_connected_markers=0`、`all_tun_interfaces=0`。随后单独再发 STOP，第 5 秒和第 10 秒 `tun0` 均不存在、dev 进程存在，恢复停止基线。该结果只说明后到的 START 可使系统标记保持，不能把它解释为最终 TUN/core 运行；未读取内部调度，不能据此证明命令有串行化、旧 STOP 被取消或全部时序安全。结合相反的快速 START→STOP 结果，XToolpro 必须将命令意图线性化并使最终停止/启动均 await 实际 TUN/health 回执。未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 重复 START 请求的最终收敛 | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt` | 冗余启动请求后的实际运行与可停止性 | Partial：停止基线下连续发出两个正确 dev START action；第 5 秒和第 10 秒 `tun0` 均存在、dev 进程存在。追加复核中连续 START 后 7 秒和 14 秒均为 `vpn_connected_markers=1`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变；单次 STOP 后 5 秒回到 `vpn_connected_markers=0`、`all_tun_interfaces=0`。随后单独 STOP 后，第 5 秒和第 10 秒 `tun0` 均不存在、dev 进程存在，恢复停止基线。固定 `handleStartAction()` 会在已存在 running request 时返回，但本轮不读取请求创建与协程调度时序；故只证明此一轮重复启动的系统标记最终可清理，不证明启动请求严格幂等、只启动一次 core、无重复 listener 或不存在资源泄漏。XToolpro 仍须以可 await 的去重/串行 command contract 及 TUN/health 回执验证。未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 重复 STOP 请求的最终收敛 | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt` | 冗余停止请求后的实际停止与 UI 进程存活 | Partial：先由正确 dev START action 建立 TUN；连续发出两个正确 dev STOP action 后，第 5 秒和第 10 秒 `tun0` 均不存在、dev 进程存在，恢复停止基线。追加复核中 START 后等待 7 秒观察到 `vpn_connected_markers=1` 但 `all_tun_interfaces=0`，随后连续发送两次 STOP 并等待 7 秒，`vpn_connected_markers=0`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变。固定 `handleStopAction()` 在尚无 running request 时返回，但本轮不读取 request 清除、service/core stop 次数或资源销毁时序；因此只证明此轮最终摘要已停止，不证明两次 STOP 严格幂等、每条 callback 都完成或无重复释放风险。XToolpro 仍须以可 await 的串行停止 contract 和实际 TUN/health 回执验证。未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 快速双 TOGGLE 的最终收敛 | `QuickActionActivity.kt`、`ServiceState.kt`、`ServiceController.kt`、`VpnService.kt` | 共享切换入口的偶数次切换最终状态 | Partial：停止基线下连续发出两个正确 dev TOGGLE action；第 5 秒和第 10 秒 `tun0` 均不存在、dev 进程存在。另先以 START 建立 TUN 后连续发出两个相同 TOGGLE action；第 5 秒和第 10 秒 `tun0` 均存在、dev 进程存在；随后单独 STOP 后 `tun0` 不存在、dev 进程存在，恢复停止基线。追加复核从停止基线连续发送两次同一 TOGGLE，7 秒后 `vpn_connected_markers=0`、`all_tun_interfaces=0`，进程和 `POST_NOTIFICATION=allow` 不变；显式 STOP 后仍为相同停止摘要。运行态追加复核中 START 后为 `vpn_connected_markers=1`、`all_tun_interfaces=0`，随后连续两次 TOGGLE 后 7 秒仍为 `vpn_connected_markers=1`、`all_tun_interfaces=0`，最终 STOP 后回到 0/0；这只能说明偶数切换保留系统标记，不能证明实际 TUN 或 core 运行。两轮结果均与偶数次切换保留各自初始最终状态相符；固定 `handleToggleAction()` 依据瞬时 `isRunningRequested()` 分派 start/stop，但本轮不读取该状态或协程调度，不能证明两个调用逐个线性化、期间无短暂反向状态或不存在资源竞争。XToolpro 必须使用线性化的显式目标状态而不是并发读取后切换，并 await 实际 TUN/health 回执。未发送流量，未读取系统 VPN/通知/日志/配置内容，保持 `Partial` |
| 始终开启、断线阻止 | Android VPN bridge、系统能力 | 能力检测、系统设置入口和明确 unavailable 状态 | Partial：ADB 可打开 Android `Settings$VpnSettingsActivity`；secure settings 显示始终开启 VPN 未配置、lockdown 未启用；固定 `VpnService.onStartCommand()` 通过受签名权限保护的 `VPN_START_REQUESTED` 广播进入 `ServiceBroadcastReceiver`，无 Flutter engine 时再读取持久 shared state、执行 `quickSetup` 并请求 native start；`onRevoke()` 先停止 service/TUN 再发送 `VPN_REVOKED`。2026-09-09 受限状态复核中，设备 `bf353dda` 在线，FlClash/XToolpro 目标进程均不存在，`POST_NOTIFICATION` app-op 为 `allow`，always-on/lockdown 为 `null`，设备端 `VPN CONNECTED` 计数与 TUN 计数均为 `0`；`ip` 接口查询被 shell 权限拒绝。固定实现未声明独立 service restart mode、可持久运行意图、retry/backoff、lockdown 检测/设置 API 或 reboot 后健康回执，故系统 always-on/lockdown 不能由该 bridge 路径推断为可用能力。未验证 FlClash 设置入口写入、断线阻止、reboot、进程死亡或竞争 VPN 行为 |
| HTTP/SOCKS/mixed 入站认证与 UDP 边界 | `core/Clash.Meta/listener/inbound/{http,socks,mixed}.go`、`listener/{http,socks}/`、`component/auth` | 代理入站认证、监听范围和 UDP 能力 | Partial：固定实现为 HTTP、SOCKS4/5 与 mixed 共用 `AuthStore`；无用户配置时使用默认/空认证存储，认证失败或协议头无效直接关闭连接。mixed 按首字节分派 SOCKS4、SOCKS5 或 HTTP，可按 `udp` 配置额外建立 SOCKS UDP listener；空监听地址归一为 `0.0.0.0`，默认 listener 还应用远端地址过滤与 skip-auth 规则。关闭操作聚合 listener 错误但没有独立 health/terminal receipt；固定 Android bridge 未形成认证凭据最小化、LAN allowlist、UDP 可用性或失败分类契约，保持 `Partial`。 |
| TUN、系统代理、局域网共享 | `android/service/src/main/java/com/follow/clash/service/VpnService.kt`、`core/`、Android bridge | 运行模式和局域网开关 | Partial：`tun0` 与真实 HTTPS 流量已验证；真机“系统代理”为开启，运行态 Android VPN `LinkProperties` 报告 loopback `7890` HTTP proxy，停止后对应代理消失；停止态显式连接该 HTTP proxy 返回状态 `000`、退出码 `7`，未保留可用监听。设备在运行态显式通过 `127.0.0.1:7890` HTTP 代理并禁用 `NO_PROXY` 访问公开 HTTPS，返回 `204`，并以同一端口的 SOCKS5 hostname 模式再次返回 `204`，证明该 loopback mixed port 可实际转发两种 TCP 代理协议；同端点 4 个并发 HTTP proxy 请求均返回 `204`。“局域网代理”为关闭时 mixed port 的 4 条 TCP 监听均仅绑定 loopback且无 wildcard；临时开启后强停重启仍为开启，运行态 mixed port 有 8 条监听且 wildcard `:7890` 计数为 1，公开 HTTPS 返回 `200`，停止并恢复关闭后再次强停重启仍为关闭。固定 bridge/配置路径未形成独立的 LAN 访问控制、认证、客户端 allowlist、UDP 能力或跨设备健康回执；静态配置模型虽定义 `mixed-port`、`allow-lan`、`bind-address`、`authentication`、`skip-auth-prefixes`、`lan-allowed-ips` 与 `lan-disallowed-ips`，但已定位的认证 middleware 属于 external controller 路径，尚不能证明 mixed proxy inbound 绑定同一鉴权合同。固定上游另有独立 UDP relay/association 处理路径，但 Android bridge、现有 proof 与停止回执没有暴露或核验该协议能力。Android `VpnService.Builder.setHttpProxy()` 仅在 API 29+ 系统代理路径设置 loopback `ProxyInfo` 与绕过域名，未提供应用侧是否遵循代理、PAC/HTTPS 语义或设置失败的独立回执。external controller 管理面虽有独立 authentication middleware，但固定 bridge 未形成 controller bind scope、secret 轮换、权限分级或管理 API 健康回执合同，不能将其鉴权/loopback 假设外推到 mixed 入站。打开该开关即改变监听范围，不能把 wildcard 监听等同于安全共享。该 proof 只验证本机 loopback TCP 功能性并发转发和监听边界，不从其他局域网设备发起连接，故不外推为 UDP、性能压力、认证、系统代理普遍生效、管理面或跨设备共享访问通过。External-controller 静态审计补充：`hub/route/server.go:92-135,162-245,249-325` 同时支持 TCP、TLS、Unix socket 和 named pipe；所有 HTTP/TLS 路径复用同一 router，secret 非空时 `authentication()` 校验 `Authorization: Bearer` 或 websocket query `token`，secret 为空则整套路由不加鉴权。默认 `ExternalControllerCors` 为 `AllowOrigins=["*"]` 且 `AllowPrivateNetwork=true`；debug 模式会挂载 `/debug` profiler。`config.go` 同时暴露 external-controller、TLS、Unix/pipe、CORS、secret、client-auth 等配置，但 `lib/common/task.dart` 仅写入 external-controller 地址，固定 Android bridge 未形成 bind scope、secret 轮换、权限分级、TLS/client-cert 健康回执或管理 API allowlist。未请求管理 API、未读取设备配置或 secret，保持 `Partial` |
| 查找进程模式 | `lib/views/config/general.dart`、`lib/providers/state.dart`、`lib/models/clash_config.dart`、`lib/models/core.dart` | 进程识别与请求归因开关 | Partial：固定源码将“查找进程”开关映射为 `FindProcessMode.always/off`，并传入 `UpdateParams.findProcessMode`/`find-process-mode`；真机初始为关闭，临时开启后强停重启仍为开启，恢复关闭后再次强停重启仍为关闭，期间未建立 VPN 且结束时 `VPN CONNECTED=0`、`tun0` 不存在。该参数路径未提供每请求归因成功/不可用状态、权限失败或性能预算；Android `PackageResolver` 的已安装包清单是独立入口，不能作为 find-process 已工作或请求字段已脱敏的证明。未验证实际进程识别、请求归因或性能开销 |
| TCP 并发 | `lib/views/config/general.dart`、`lib/providers/state.dart`、`lib/models/clash_config.dart`、`lib/models/core.dart` | TCP 连接并发参数 | Partial：固定源码将“TCP 并发”绑定到 `state.tcpConcurrent`，并传入 `UpdateParams.tcpConcurrent`/`tcp-concurrent`；真机初始为开启，临时关闭后强停重启仍为关闭，恢复开启后再次强停重启仍为开启，期间未建立 VPN 且结束时 `VPN CONNECTED=0`、`tun0` 不存在。该路径只有布尔设置持久化，没有 core 接受/拒绝回执、有效范围/资源预算或运行时并发指标；既有 4 路 loopback 请求 proof 未隔离该开关因果，故未验证实际并发连接行为或性能变化 |
| 统一延迟 | `lib/views/config/general.dart`、`lib/providers/state.dart`、`lib/models/clash_config.dart`、`lib/models/core.dart` | 延迟测量显示策略 | Partial：固定源码将“统一延迟”绑定到 `state.unifiedDelay`，并传入 `UpdateParams.unifiedDelay`/`unified-delay`；真机初始为开启，临时关闭后强停重启仍为关闭，恢复开启后再次强停重启仍为开启，期间未建立 VPN 且结束时 `VPN CONNECTED=0`、`tun0` 不存在。固定路径未提供 core 参数接收 ack、握手/连接阶段分解、有效范围或超时/取消/失败回执；既有测速结果未隔离该开关两态，不能证明延迟值实际去除了握手等额外延迟或改变排序因果 |
| 测速链接与延迟测试输入 | `lib/views/config/general.dart`、`lib/providers/config.dart`、`lib/providers/actions/proxies.dart`、`lib/core/interface.dart`、`lib/common/compute.dart` | 测速 URL 配置与校验 | Partial：固定源码将“测速链接”绑定到 `appSettingProvider.testUrl`，要求非空且通过 URL 校验，并在代理测速链路作为 `test-url` 参数传给 core；真机对当前公开测试链接打开编辑对话框，非法输入显示“测速链接必须为 URL”，取消后原值仍为 `https://www.gstatic.com/generate_204`。随后保存公开 Cloudflare 204 URL，强停重启后基本配置页仍显示该 URL；建立 VPN/TUN 后从代理页触发延迟测试，得到 6 个可见延迟值 `57`、`69`、`91`、`96`、`104`、`138 ms`，之后停止 VPN 并通过“重置”恢复默认公开 URL。固定失败路径会把代理名和原始异常传入日志，未见稳定的 URL 脱敏、目标归属、超时/取消或逐节点 terminal receipt；未捕获核心请求，不能独立证明每个测速请求的实际目标、任意 URL 的安全边界或全部节点/组的行为 |
| IPv6、DNS、Fake-IP/Host、流量嗅探 | `lib/views/config/network.dart`、`lib/views/config/general.dart`、`lib/providers/actions/setup.dart`、`lib/common/task.dart`、`android/service/src/main/java/com/follow/clash/service/VpnService.kt`、`core/Clash.Meta` | DNS/IPv6/嗅探配置和诊断 | Partial：真机确认 `IPv6=false`、`DNS 劫持=false` 的关闭边界；临时开启 DNS 劫持后强停重启仍为开启，源码将开启值传为 `Core.startTun(dns = "0.0.0.0")`，同时 Android VPN 仍发布固定 DNS stub；开启态 `tun0` 有 1 条 IPv4、0 条全局 IPv6 地址，系统 VPN、DNS stub 与 `tun0` 各匹配 1 项，公开主机名解析成功且 HTTPS 返回 `200`；停止并恢复关闭后再次强停重启仍为关闭。临时开启 IPv6 后强停重启仍为开启，开启态 `tun0` 有 1 条 IPv4 和 1 条全局 IPv6 地址，VPN `LinkProperties` 含 IPv4/IPv6 DNS stub 各 1 项、`::/0` 路由匹配 2 项，公开 HTTPS 返回 `200`；停止并恢复 `IPv6=false` 后再次强停重启仍为关闭。追加系统 DNS 开关初始为关闭，临时开启后强停重启仍为开启；开启态 `tun0` 地址行数为 2、系统 VPN 为 1、IPv4 DNS stub 匹配为 1、IPv6 DNS stub 匹配为 0，公开 HTTPS 返回 `200`，随后恢复关闭并再次强停重启仍为关闭。固定源码把该开关传入 profile 构造，并向 `rawConfig['dns']['nameserver']` 追加 `system://`；本轮未读取生成配置，不能证明该值已实际写入最终 nameserver 列表。强制指定 DNS 服务器的请求在 DNS 劫持两态均成功，不能区分 TUN 捕获与代理侧解析；固定路径也未提供 Fake-IP/Host/嗅探的独立能力探测、运行回执或稳定错误映射。静态补充：`core/Clash.Meta/component/fakeip` 通过 `profile/cachefile` 将 host↔fake-IP 映射和偏移持久化到 bbolt；`cache.go` 以 `0666` 创建 cache 文件，遇到 `ErrInvalid`/checksum/version mismatch 会删除现有 cache 后重建，并将完整 cache path/raw error 写入日志。`dns/client.go`、`dns/util.go` 会把 DNS server 地址、查询名、ECS 前缀、截断重试和缓存异常写入 debug/warn 日志。sniffer dispatcher 支持 HTTP/TLS/QUIC，按端口、源/目的地址、域名和 force/skip 规则读取首包并改写 `Metadata.Host`/`SniffHost`/`DstIP`；失败跳过缓存为 128 项、600 秒 LRU，但日志包含源/目的地址、sniff host 和失败原因，未见字段脱敏、sniff timeout/取消、稳定错误映射或对外 capability/health/terminal receipt。platform hosts resolver 还会每 5 秒以 mtime/size 缓存完整 `/etc/hosts` 等平台文件的 host/IP 映射；无文件或无权限时静默得到空映射，其他读取错误也没有稳定 `Unavailable` 回执，且未见大小上限、来源/version、显式清除或隐私边界合同。DoH/DoT/DoQ 静态补充：固定归档 `dns/doh.go`（SHA-256 `4B37A02973B0E97C94FEEDCF1979B9E821D798BC541ED95B7C474EC50005CA24`）、`dns/dot.go`（`02FB4DA8A303AC26E68C591A48ADB931D6ACB09ED7F2E7DB87A5C6B2448B788C`）和 `dns/doq.go`（`0E41381374782B541AA875AD1D3751D25F7151823C061E2FDD7221327C413588`）显示：DoH 使用 context-bound HTTP 请求、10 秒拨号/TLS 握手预算、5 分钟 idle/30 秒 ping 参数、TLS 1.2 minimum、连接复用与超时/QUIC/0-RTT reset 后最多两次重建重试；DoT/DoQ 使用 context-aware 拨号、默认 5 秒 DNS timeout、复用连接失败重试和显式 Reset/Close，DoQ 也区分 idle/stateless reset 等可重试错误。三者接受 `skip-cert-verify` 与 `name-cert-verify` 参数，但固定路径没有 XToolpro-facing 证书策略、取消/超时/错误分类、每上游 health/capability 或终端回执，且连接/拨号错误日志可能包含上游地址、耗时和原始错误。真实 DoH/DoT/DoQ、证书失败、取消、重试与隐私边界验证前保持 `Partial`；故这些语义仍未验证 |
| 按应用代理/绕过 | `core/`、`lib/views/access.dart`、Android `AppPlugin`/`PackageResolver` | 应用选择与路由策略 | Partial：真机进入访问控制页，页面说明选中应用将被排除在 VPN 之外；“允许应用绕过 VPN”初始为开启，临时关闭后强停重启仍为关闭，关闭态可建立并停止 `VpnService`/`tun0`，恢复开启后再次强停重启仍为开启；固定源码把该值传入 `VpnOptions.allowBypass` 并在建 VPN 时调用 `VpnService.Builder.allowBypass()`。2026-09-07 当前固定包在目标设备未获取到可选应用列表，ADB UI tree 仅见页面说明、无应用条目或错误/重试控件；包已声明且被授予 `QUERY_ALL_PACKAGES`。固定源码显示 Android bridge 用 `getInstalledPackages(GET_PERMISSIONS)` 生成包含包名、标签、系统/网络标记和更新时间的完整 JSON 清单，未映射异常为稳定错误；同一 `AccessView` 实例只在初始化时加载一次，空结果直接渲染“无数据”，没有错误态或重试入口。选择包名列表与 `allowBypass` 是独立输入，固定启动 shared state 会携带 accept/reject 包名与开关，但没有按应用 route/permission/版本回执或原子更新结果。无法在不读取应用清单或原始诊断的前提下判定是 MIUI、包查询、bridge 或解析的具体根因。该清单属于敏感设备应用信息，XToolpro 未来仅可在用户进入该流程后最小化、内存内使用，禁止日志、导出、遥测或默认持久化。Android 13 `dumpsys` 未暴露 `allowBypass` 字段，故未选择应用或验证应用主动绕过后的实际流量路径 |
| 域名/IP/GeoIP 规则集和命中日志 | `core/` | 规则管理、命中详情和脱敏日志 | Partial：真机确认附加规则入口和规则模式；用户在设备侧启用日志捕获后，规则模式 VPN/TUN 运行期间的一次公开 HTTPS 请求返回 `204`，并在设备日志页人工确认新增记录。固定请求模型可携带 `rule`/`rulePayload` 等字段，但未见规则版本、命中原因与请求事件的稳定关联回执；ADB 仅确认日志入口/系统 VPN 状态，不读取或导出日志正文、规则、节点或配置；尚未证明具体规则条目命中或命中详情。GeoIP/GeoSite/GeoData 静态审计补充：`update_geo.go:52-194` 通过可配置 `geox-url` 创建 HTTP vehicle，使用固定 90 秒上下文超时、2xx 状态检查、内存 hash 去重和 GeoIP/GeoSite loader 解析校验后才写入；但 `sizeLimit=0`，响应完整读入内存，未见 Content-Type、长度上限、签名/版本 provenance 或独立取消令牌。`resource/vehicle.go:38-47,114-116` 的 `safeWrite` 直接 `os.WriteFile` 覆盖目标，没有同目录临时文件、fsync、原子 rename、read-back 或失败回滚；初始化 `geodata/init.go:71-87,102-110,131-160,180-187` 还在无效数据时先删除现有文件再直接下载覆盖，失败可能留下缺失或部分文件。`UpdateGeoDatabases` 用单个进程级布尔防并发，并以 errgroup 并行更新多个资源；定时器按首个存在文件的 mtime 判断整体新鲜度，未见每资源 terminal receipt、持久化调度恢复或来源锁定。Flutter `GeoResourceAction` 只校验 URL 形状并转发更新，未建立 UI 级成功/失败/取消回执。规则提供者静态审计补充：`rules/provider/parse.go` 支持 `file`、`http`、`inline` 三类 vehicle、可选 `size-limit`、header/proxy、interval 和 bundle fallback；HTTP 路径按 URL hash 生成缓存文件。`component/resource/fetcher.go` 使用可取消 context、周期 pull 和失败 backoff，在 parser 成功后才调用 vehicle.Write，因此解析失败通常保留旧内容；但 `HTTPVehicle` 以 `io.LimitReader` 截断而不检测超限，写入仍是直接覆盖，未见原子发布、fsync、read-back、签名/版本 provenance。`rules/provider/provider.go` 的 `onUpdate` 直接替换 strategy 并触发全局 callback，未见 provider 级锁、逐项更新 receipt、稳定错误类别或 UI 取消状态；rule-set 命中在 provider 缺失时静默返回 false。未发起下载或修改设备数据，保持 `Partial` |
| rule-provider 车辆与 `/rules` 命中 API 契约 | `core/Clash.Meta/rules/provider/{parse,provider,rule_set}.go`、`core/Clash.Meta/hub/route/rules.go` | 规则源更新、命中统计和禁用操作 | Partial：固定解析器支持 `file`、`http`、`inline` vehicle，yaml/text/mrs 格式、`interval`、`size-limit`、header/proxy 与 bundle fallback；成功 parse 后替换 strategy 并触发 RuleUpdateCallback。`ParseRule` 支持 DOMAIN/GEOSITE/GEOIP/IP-CIDR/IP-ASN、RULE-SET 与 AND/OR/NOT 组合；`/rules` 返回 index/type/payload/proxy/size 及 disabled/hitCount/hitAt，PATCH `/rules/disable` 按 index 修改运行态。未见签名/来源绑定、原子 staging/read-back、规则版本 hash、逐项更新 receipt、稳定错误分类或 payload/host 脱敏；provider 缺失时 rule-set 静默返回 false，保持 `Partial`。 |
| 实时上下行速率、会话流量、连接列表 | `core/Clash.Meta/tunnel/statistic`、`hub/route/server.go`、`hub/route/connections.go`、service bridge | 实时状态、历史摘要和任务事件 | Partial：固定 `statistic.Manager` 分别维护瞬时速率、全局累计字节及按是否 DIRECT 区分的 proxy 累计；`/traffic` 每秒推送 `up/down/upTotal/downTotal`，连接 REST 返回快照，WebSocket 默认 1000ms 轮询且可由 `interval` 调整，单条/全部关闭返回无正文 204。Flutter 连接模型包含 UID、进程/路径、host、源/目的 IP/端口、规则与 payload、GeoIP/ASN、DNS 模式、特殊代理/规则和远端目的地；连接页轮询并提供单条/全部关闭。未形成字段最小化、脱敏、关闭成功回执、累计清零与请求缓存同步清理的统一合同，保持 `Partial`。 |
| 请求/连接列表详情字段与筛选 | `lib/views/connection/connections.dart`、`lib/views/connection/item.dart`、`lib/views/connection/requests.dart`、`lib/models/common.dart`、`lib/core/interface.dart`、`lib/manager/core_manager.dart`、`lib/providers/actions/setup.dart`、`lib/providers/actions/core.dart`、`lib/providers/app.dart`、`core/hub.go` | 请求/连接详情、搜索筛选和隐私边界 | Partial：固定源码的 `TrackerInfo`/`Metadata` 模型包含 UID、进程与进程路径、网络类型、源/目的 IP 与端口、host、规则及 payload、代理链、源/目的 GeoIP、ASN、DNS mode、special proxy/rules 和 remote destination；连接页每秒调用 `coreController.getConnections()` 拉取完整列表，详情页直接展示上述字段。列表搜索仅覆盖 network、host、destination IP、process 和 proxy chains，未覆盖源/目的端口、规则 payload、GeoIP/ASN、DNS 或 remote destination；读取异常会将原始 `$error` 写入 `commonPrint`。core request event 会把完整 `TrackerInfo` 追加至常驻内存 `requestsProvider`；`fullSetup()`（当前 Profile ID 变化）重置为容量 500 的列表，未见持久化或用户清除入口，进程重建/重新 setup 即丢弃已有记录。普通 `setRunning(false)` 只停止 listener、重置流量并清空 traffic 状态，未清空 requests；随后启动、VPN 配置变更触发的 stop/start，以及 core restart 后重放的 `setRunning`/`applyProfile` 均不调用 `fullSetup()`，因此在 Flutter app process 存活时这些路径不会按此代码清理既有请求缓存。连接页单条/全部关闭没有确认或稳定 UI 结果；“全部”未 await core 调用，core 又忽略逐条关闭错误后仍返回成功。该缓存有界但无字段最小化或脱敏，关闭操作也无可验证结果。本轮未读取设备连接正文或网络标识，仅复核固定源码并做无内容 ADB 状态确认；能力入口与生命周期可确认，但尚无字段级脱敏、最小化返回、普通停启后的设备清理、搜索/详情一致性或稳定错误映射证据，保持 `Partial` |
| 请求事件回调与停止确认 | `lib/manager/core_manager.dart`、`lib/core/lib.dart`、`lib/views/dashboard/widgets/core_status_button.dart`、`android/app/.../plugins/ServicePlugin.kt`、`ServiceState.kt`、`ServiceController.kt`、`android/service/.../VpnService.kt`、`android/core/.../core.cpp`、`core/lib.go`、`core/hub.go`、`core/message.go` | 停止完成、事件入口冻结与诊断队列清理 | Partial：Flutter `ServicePlugin.stop()` 仅发起 `ServiceState.requestStop()` 后立即向 MethodChannel 返回 `true`，不等待其 `Deferred<Boolean>`；实际 stop 在后台协程执行。后续 `ServiceController.stop()` 会同步调用 bound service 的 `stop()`，VPN service 路径会尝试停止 modules、调用 `Core.stopTun()` 并执行 `stopSelf()`，但 controller 对调用失败只记录日志，仍清空 binding、将 run time 归零；`ServiceState` 随后直接设为 `STOPPED`，没有核验 Android service 已销毁、TUN 已消失或 core 已健康停止的回执。native `stopTun()` 会关闭 TUN handler 并同步调用 `handleStopListener()`，但不会清空 RequestMessage 所在的全局 bulk queue；常驻 batcher 仍可将停止前已入队的批次交给未解除的 event listener，存活的 `CoreManager` 仍会消费其中的 request。engine detach 时 `setEventListener(null)` 会释放 JNI global reference，但固定 Go/C++ 路径未显示与 batcher 的 listener 读取、JNI 回调之间的生命周期串行化或 in-flight 等待；这是静态并发风险，不能外推为设备已发生崩溃或事件泄露。停止态真机的 dashboard 核心状态按钮仅通过父节点暴露语义；按下后立即用 Android BACK 取消，复核 `tun0` 与系统 VPN 标记仍均不存在。源码确认该入口只有确认结果为 true 才进入 `restartCore()`。该结论仅证明缓冲事件可能延迟送达，不推断 stop 后产生新的 request event。停止返回、系统实际停止和事件/诊断队列清理仍不是同一可验证合同。XToolpro 必须 await 并核验实际停止结果，以单一生命周期锁或串行执行器停止事件生产、drain/drop 队列、解绑消费者、等待 in-flight callback 后再释放 JNI/engine 引用，并以无敏感字段的设备断言覆盖成功、失败、超时、取消、engine crash、detach/restart racing 与 version mismatch；在此之前保持 `Partial` |
| 请求、规则、内核日志与崩溃诊断 | `core/`、service bridge、`android/common/GlobalState.kt` | 脱敏诊断导出 | Partial：直连公开 HTTPS 流量后请求页显示 10 个可见记录，其中 6 个 accessibility 节点带 `DIRECT` 路由语义；临时启用 `info` 和日志捕获后日志页显示 6 条可见/部分可见 `info` 记录，随后恢复原设置；规则模式早期重测返回 HTTP `000`，但用户在设备侧选择可用路径后，规则模式 VPN/TUN 建立且公开 Cloudflare 204 HTTPS 返回 `204`，请求页可见且未出现 `DIRECT` 路由语义。随后用户开启日志捕获，规则模式下另一公开 HTTPS `204` 请求后，在设备日志页人工确认新增记录；强停重启后 VPN 未自动建立且“日志”入口仍可见。ADB UI tree 只暴露该入口，未读取或导出任何日志正文，也不能直接读取日志捕获开关值。该结果证明真机可见的日志捕获事件与入口跨进程保留，不证明具体规则命中详情、日志字段完整性或崩溃诊断。源码隐私审计：固定 Android 模块引用 Firebase Analytics 与 Crashlytics NDK，服务同步时将 Flutter `crashlytics` 设置传给 `FirebaseCrashlytics.isCrashlyticsCollectionEnabled`；Dart 默认值为关闭，但应用启动的“上次崩溃”查询仍会初始化 Firebase，且 manifest 未提供静态默认禁用声明。固定提交 `62addf738a76b1a492e19af2dbabdb6d572b9e72` 的 `core/hub.go`、`core/lib.go`、`core/constant.go`、`core/Clash.Meta/tunnel/statistic/manager.go`、`lib/core/event.dart`、`lib/manager/core_manager.dart`、`lib/common/print.dart`、`lib/providers/app.dart`、`lib/views/logs.dart` 和 `lib/views/connection/item.dart` 显示：core log/request 事件进入容量 256、批量 32 的 bulk 队列，满载丢弃同类最旧事件；无 listener 时批次丢弃，存在 listener 时 JSON 序列化后回调 Dart。`openLogs` 只控制 core log capture，app attach 后 `commonPrint` 仍把原始文本写入 `debugPrint` 与最多 500 项内存日志；日志页可选择正文并可经 SAF 导出全部 `Log.toString()`。request tracker 对外携带 process、host、源/目的 IP、DNS mode、rules、rulePayload、chains、remote destination 等字段，详情页直接展示。`handleCrash()` 直接 panic；Android `GlobalState` 使用 `Log.d`，服务/bridge 可能把原始 error message 送入日志、Toast 或 MethodChannel。固定源码未发现直接把 URI 或配置提交给 Crashlytics 的调用，但“上次崩溃”查询仍初始化 Firebase，未形成默认禁用/上传边界，也未形成 crash terminal receipt 或队列 drain/drop 可核验结果。XToolpro 不得直接复用该第三方遥测路径；必须默认不初始化/不上传，在显式同意后才启用，并在日志、请求、崩溃和导出边界统一字段最小化、脱敏稳定错误码与逐项 terminal receipt；完成对应契约测试前保持 `Partial` |
> 2026-09-09 checkpoint：停止路径复核确认全局 priority/bulk channel 未由 `stopTun` 关闭，batcher 没有显式 drain/drop 完成回执；listener 释放与批量回调也未见 in-flight 串行化。本项继续保持 `Partial`。
| 内核版本、更新和回滚 | `lib/views/about.dart`、`lib/common/request.dart`、`android/core/src/main/cpp/CMakeLists.txt` | 组件管理、校验和、回滚 | Unavailable（固定 Android 包）：真机关于页仅显示应用版本；“检查更新”检查 FlClash 应用 Release，“内核”只打开上游源码链接；未提供运行时内核版本、内核更新或内核回滚控件，`libclash.so` 在构建期链接。固定路径未提供 native artifact 版本/hash 清单、热替换、last-known-good 回滚或失败 terminal receipt；内核更新只能随受审计的 engine/APK 发布 |
| Native core/bridge ABI 配对与版本不匹配拒绝 | `plugins/setup/buildkit`、`.github/workflows/build.yaml`、`android/core/src/main/cpp/CMakeLists.txt`、`core.cpp`、`Core.kt`、`ServiceController.kt`、`VpnService.kt`、`jniLibs/<ABI>/libclash.so`、`cpp/includes/<ABI>/` | 启动前完整性校验、`VersionMismatch` 与受配对 APK 回滚 | Partial（静态）：上游 buildkit 的 Android target 表定义 `armeabi-v7a`、`arm64-v8a`、`x86_64` 三种 c-shared core，未指定 arch/target-platform 时构建全部；Gradle setup task 在 CMake 配置/原生构建前执行该步骤。固定 CI 的 Android job 同样未传 arch，调用 `dart setup.dart android`，其 packaging 侧启用 `split-per-abi`；但后续 F-Droid 投递和 Telegram release 筛选均只匹配 arm64 命名。这只是构建/投递声明，既不证明其他 APK 已生成或发布，也不形成任何已发布 ABI 的 hash、符号、bridge、commit 或健康 manifest。buildkit 会将 Go 源、依赖锁、build-tool 源、编译器/NDK 与 Go 环境写入本地 build-cache fingerprint，但其 record 对输出只保存路径、大小和修改时间，且 build flags 仅为 `-w -s`；该临时构建缓存不是随 APK 交付或由 runtime 核验的 provenance manifest。当前固定归档只含 `arm64-v8a` 输入，故其余 ABI 不能由该归档或一次 arm64 proof 推断。现有固定 dev APK 解包也仅有 `arm64-v8a`；其中 `libclash.so` SHA-256 为 `859D…EE29F`、`libcore.so` 为 `D352…63FC`，分别包含预期 Go export 与 Kotlin JNI bridge 符号，且 ELF 为 AArch64。`libcore.so` 的 dynamic table 明确 `NEEDED libclash.so`，其预期 core symbols 保持 unresolved import，故该一份 APK 的链接对存在；仍不含 FlClash/Clash.Meta commit、bridge API 或完整 manifest，不能外推其他 ABI 或运行健康。对每一 ABI，CMake 仅在同时存在 `libclash.so`、`libclash.h` 与 `bride.h` 时定义 `LIBCLASH` 并链接 core；缺少任一文件仍会构建 `libcore.so`。该 fallback 保留同一 JNI 表面，但 `startTun`、`stopTun`、`quickSetup`、事件和 DNS 等调用为空操作，流量查询只返回 `{}`。Kotlin 无条件加载 `core`，其 JNI 与生成头文件均未暴露/核对 FlClash commit、Clash.Meta commit、ABI、bridge API 或 native artifact hash。原生 `UnsatisfiedLinkError`、`ExceptionInInitializerError` 或其他 `LinkageError` 会越过 `VpnService` 的 `catch (Exception)`，再被外层 `runCatching` 泛化为 `ServiceController.start()` 的 `0L`；`ServiceState` 因而只转为 `STOPPED`/布尔 `false`，而 `ServicePlugin.start()` 早已向 Dart 返回 `true`。没有显式 `EngineCrashed`/`VersionMismatch` 分类或脱敏对外回执。更严重的是 fallback 本身不抛错：服务可在 `Core.startTun()` 空返回后保留 TUN，而 `quickSetup()` 回调可能永不完成。故“构建成功”或 `System.loadLibrary` 成功不能证明已加载锁定的完整 native core，bridge/core 漂移也不会由该路径明确映射为 `VersionMismatch`。XToolpro 必须在任何配置写入、导出或 VPN 请求前，逐个已发布 ABI 核验受签名 manifest 中的 FlClash/Clash.Meta commit、engine API、ABI、bridge 与 `libclash.so` SHA-256、必需头/符号集合；任一缺失、ABI 不符或不一致均返回脱敏 `Unavailable`/`VersionMismatch` 并阻止启动，并将加载、符号、回调超时和运行健康失败分别映射为稳定的 `Unavailable`/`EngineCrashed`。实际完整 core 与刻意缺失/错配 artifact 的隔离契约测试尚未执行，保持 `Partial` |
> 2026-09-09 checkpoint：native core/bridge 配对复核仍未执行刻意缺失或错配 artifact 的隔离契约测试；本项状态继续保持 `Partial`，不改变 Phase 02 gate。

| XToolpro `engine-proxy` 适配器与五类契约执行 | `engine-proxy/build.gradle.kts`、`engine-proxy/src/main/AndroidManifest.xml`、`core-model`、`docs/architecture/engine-contract-test-plan.md` | 版本化 adapter、capability/health、`Success`/`Unavailable`/`Cancelled`/`EngineCrashed`/`VersionMismatch` | Partial（Phase 02 gate 静态审计）：当前受版本控制的 `engine-proxy` 仅含 Android library Gradle 壳和空 manifest；没有 Kotlin/Java 源文件、FlClash 依赖、native artifact、公开 contract、health/version handshake 或测试源。`core-model` 仅定义通用 `ModuleId.Proxy`，未提供 Proxy engine 结果模型。2026-09-09 对计划、Gradle、manifest 和 `ModuleId.kt` 的 SHA-256 复核分别为 `20161EFB1E7DFCA70CD31F88CA504328E172B0E534D7275F05E038D1E3BC77F6`、`5A152141FE75A58515AC09C45ED9A6EEB623767CCB44BF25C4299CE0E7D2CC65`、`571F2735FAF5E857752EE057AC8EF63425576A721616C60B86EE5D4C0A2D682F`、`80B8B427F487858772F3D9FBEFD3878BDD7855AD4AFDB0FDD06628E87B0610ED`；计划虽定义五类场景，但仓库没有可执行 adapter/test source，不能把计划、build 目录产物或既有上游真机观察当作 XToolpro contract 执行证据。依 Phase 02 范围，本轮不新增 adapter/SDK/cache/upstream artifact；未来实现须先完成完整 native/bridge 配对、受签名版本 manifest、脱敏错误映射和隔离契约测试，再进入正式 engine 集成。保持 `Partial`，Proxy 台账保持 `Investigating` |
| 启动连接、自动更新、快捷方式/小组件 | `android/`、`QuickActionActivity`、`TileService`、`ServiceBroadcastReceiver`、`lib/manager/tile_manager.dart`、平台集成 | 后台入口和设备能力开关 | Partial：真机临时开启“自动运行”后，应用强停再打开会自动建立 `VpnService`、`tun0` 和系统 VPN；恢复关闭后再次强停再打开保持停止，确认开关行为和持久化；自动检查更新开关可见，手动更新检查完成 Release 元数据查询并返回当前应用已是最新版，未进入下载/安装分支。固定源码在应用初始化时通过 `ShortcutManagerCompat` 注册动态 `toggle` 快捷方式，真机启动主界面后系统仅按该固定 ID 计数为 1，且 `VPN CONNECTED=0`、`tun0` 不存在。导出 `QuickActionActivity` 将 START/STOP action 分派至 `ServiceState`；真机在 VPN 停止态触发 START 后，系统报告 `VPN CONNECTED=1`，但 14 秒内始终未见 `tun0`，故未发起流量测试。再次先初始化主界面再触发同一 action，7 秒后仍为 `VPN CONNECTED=1`、`tun0` 不存在，排除冷启动时机作为该现象的充分解释；随后 STOP action 均使 `VPN CONNECTED=0`、`tun0` 不存在。强停固定包后直接目标启动同一 START action，20 秒后仍为 `VPN CONNECTED=1`、无 `tun0`，以 STOP action 又在 5 秒后恢复停止；固定源码仅在 `MainActivity` 附着 Flutter engine，此 intent 不启动该 Activity，故已覆盖原生 fallback 但仍未形成 TUN。固定 Android manifest 还声明了 `TileService`；在停止态临时添加并点击该精确系统快捷设置磁贴后，7 秒和 14 秒均为 `VPN CONNECTED=1`、无 `tun0`，未发起流量。第二次磁贴点击及移除磁贴后请求仍未自行清理，已用既有固定 STOP action 恢复为 `VPN CONNECTED=0`、无 `tun0`。源码显示 QuickAction 与磁贴共用 `ServiceState`：engine 已附着时该路径仅经 `TilePlugin` 向 Dart `TileManager` 的监听者发送 `start`/`stop`，并跳过 Android 偏好读取、core setup 和 `ServiceController.start`；`TilePlugin` 仅作无 response 的 MethodChannel 投递，没有持久队列、交付确认或 engine detach 后重放，故 Activity/engine 销毁或 Dart listener 未就绪时无法判定启动意图是否交付或应否 native fallback。无 engine 时才走原生 fallback。所有 QuickAction 分派又使用无父生命周期的 `GlobalState` supervisor coroutine 后立即 finish Activity，调用方没有 await/result/timeout；子协程异常不会转换为入口层稳定 `Cancelled`/`Unavailable`/`EngineCrashed`。`VpnService` 只有在 `Builder.establish()` 取得 descriptor 后才调用 `Core.startTun`，但上层 `ServiceController` 的运行状态不独立核验 `tun0`、core 健康或可转发流量；故系统 VPN 连接不能作为任一入口可用的充分证据。固定 `ServiceBroadcastReceiver` 虽为导出 receiver，但以 `${applicationId}.permission.RECEIVE_BROADCASTS` 的签名级权限保护，且只接受 `VPN_START_REQUESTED`、`VPN_REVOKED`；这条系统回调边界与无权限的 `QuickActionActivity` 不同，不能降低后者的跨应用状态变更风险。完整 manifest 审计显示 `TileService` 以 `BIND_QUICK_SETTINGS_TILE` 受系统绑定保护，`VpnService`、`ProxyService` 均不导出；但磁贴点击仍显式转到无权限 `QuickActionActivity`，系统 service 的绑定权限不能覆盖这条下游状态变更入口。`quickSetup` 非空返回值和启动异常还会未经脱敏进入 Android `Log.d`，前者同时进入 Toast；不得将其作为 XToolpro 的 UI、默认日志、诊断导出或遥测字段。`QuickActionActivity` 又是无组件权限的导出 Activity，且未验证调用者，仅按 action 调度 START/STOP/TOGGLE；XToolpro 不得复用该跨应用状态变更入口。该 UI 生命周期依赖与真机现象一致，但静态源码不能确定已附着分支试验时的 engine/事件状态，未形成根因证明。该结果只证明动态快捷方式、action 与磁贴启动入口可达，不证明任一入口可独立完成可用 VPN/TUN 生命周期；固定 Android 包未声明静态快捷方式或 AppWidget，相关能力为 Unavailable |
| 前台 VPN 通知停止动作 | `android/service/.../NotificationModule.kt`、`android/common/.../Ext.kt`、`QuickActionActivity` | 脱敏前台通知与安全停止入口 | Partial：固定源码以 `PendingIntent.FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT` 创建通知 action，系统交付时不能由接收方篡改该 intent；但停止按钮仍显式指向无 permission 且导出的 `QuickActionActivity`，同一 `STOP` action 可被第三方直接调用。immutable pending intent 仅收窄 notification token 的篡改面，不能修复该独立导出组件的跨应用 VPN 状态变更面；XToolpro 必须改为不可导出或签名保护的内部停止入口，并以独立 immutable pending intent 绑定。真机本轮仅复核 `VPN CONNECTED=0`、`tun0=0`，未读取通知内容或与通知交互 |
| Android 13+ 前台通知权限门控 | `android/app/.../AndroidManifest.xml`、`AppPlugin.kt`、`ServiceState.kt` | VPN 启动前权限、拒绝与 unavailable 状态 | Partial：固定 manifest 声明 `POST_NOTIFICATIONS`；源码在启动路径请求该权限，但 permission callback 不检查 `permissions` 或 `grantResults`，而是将进程内 skip 标志置位并无条件继续启动；没有附着 Activity 时同样直接继续。该 plugin 只有一个可变 callback 槽：新请求会先以 `false` 结束旧 callback 并替换槽位；若系统 dialog 已在进行中，新请求不再发起请求而直接返回，系统回调最终无条件以 `true` 完成最新槽位。因此并发/重入请求既没有独立 permission transaction，也不能把真实授权结果可靠对应到原始启动意图；activity/engine detach 只会以 `false` 结束当前槽位。configuration change 则只清空再设置 Activity，并保留 callback/请求标志；源码没有可持久恢复 transaction、timeout 或状态映射，旧 Activity dialog result 向新 binding 的可靠交付尚无真机契约。任一 permission callback 后 `skipNotificationPermissionRequest` 只会置为 `true`、无复位；其后的 request 因此即使 `checkSelfPermission` 已反映撤销，也会绕过重新请求并直接成功。`ServiceState.requestStop()` 只替换 run token，不会取消或完成正在等待 notification callback 的 `requestStart()`；该 Deferred 会一直等待 system result/detach，虽然后续 `start()` 会因 token 已失效拒绝启动，仍没有有界 `Cancelled` completion。API 33 真机停止基线下，以正常 `pm revoke` 临时撤销 dev variant 的该单一权限后，UID 层 `POST_NOTIFICATION` 为 `ignore`（同次 package 层查询仍显示 `allow`，故不将该输出简化为单一 app-op 值）；强停并以正确 dev 原生 START action 启动，7 秒和 14 秒均未建立 TUN、进程存在。另在相同撤销态先启动 dev `MainActivity` 8 秒再触发同一 action，第 7 秒和第 14 秒同样未建立 TUN；在另一 attached-activity 轮中，START 后 2 秒发送系统 BACK 取消尝试，第 7/14 秒也未建立 TUN、进程存在。另一次同类 START 后 2 秒强停，5 秒后 TUN 与进程均为 0；随后 launcher 重开后 8 秒仍未观察到 TUN 或进程，不能据此断言 UI/render 或 callback 行为。因不读取窗口内容，各轮均不能断言有 permission dialog 或 callback 已执行。各轮均以 STOP 保持无 TUN、正常 `pm grant` 恢复为 allow。结果不读取通知正文且不能证明通知可见性、permission callback 分支、用户可见 unavailable/cancel 映射或完整 VPN/core 健康。XToolpro 必须以真实授权结果、每次请求的可取消 completion 和前台通知可见性为门控，拒绝时保持不启动并给出可恢复状态；不得复用该 unconditional-continue/单槽分派。 |
| 通知权限人工“允许”与实际授权核验 | `AppPlugin.kt`、`ServiceState.kt`、Android runtime permission/app-op | callback、系统授权与 TUN 启动的可归因结果 | Partial：API 33 撤销 `POST_NOTIFICATIONS` 后从主界面触发启动，用户报告选择“允许”；随后 0 秒与 7 秒只读核验仍为 `granted=false`、UID app-op `ignore`、`tun0=0`、dev 进程存在。未读取任何窗口或通知内容，不能将该不一致归因于具体系统 UI、callback 或 app 分支；但说明用户操作、native callback、权限生效与实际启动之间没有可观察的统一回执。随后正常 `pm grant` 与 STOP 恢复为 `granted=true`、`allow`、无 TUN。XToolpro 必须以实际 permission/app-op 结果与 TUN/health 回执而不是操作意图或 MethodChannel 成功决定状态；保持 `Partial` |
| VPN service 断开、进程死亡与系统恢复 | `VpnService.kt`、`ServiceController.kt`、`ServiceState.kt`、`ServiceBroadcastReceiver.kt` | 持久状态机、故障收敛与系统恢复 | Partial：固定 `VpnService.onRevoke()` 会停止 service/TUN 并通知应用层；绑定 service 的断开、binding died 或空 binder 会清理 binding、将运行时归零并把 `ServiceState` 置为 `STOPPED`，但没有 retry/backoff、持久化错误原因或自动恢复。系统 always-on 的 `onStartCommand()` 仅发送内部启动广播后委托 `super` 返回，随后依赖已持久化 shared state 重新 setup/start；固定代码未声明明确 start-mode 或可验证的进程死亡恢复合同。真机仅确认停止态 `VPN CONNECTED=0`、`tun0=0`、进程数 `0`；未触发杀进程、系统重启、always-on/lockdown 或权限撤销。XToolpro 必须以独立持久状态机记录运行意图、失败原因和恢复条件，并分别验证 service loss、process death、reboot、VPN revoke 与未获通知权限的恢复行为 |
| 前台服务类型与 VPN 组件边界 | `android/service/src/main/AndroidManifest.xml`、`android/common/.../Ext.kt` | Android 前台服务合规声明与启动调用 | Verified（静态）：`VpnService`、`ProxyService` 均为 `exported=false`；两者声明 `foregroundServiceType="specialUse"`，并分别设置 `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` 为 `vpn`/`proxy`。API 34+ 的共享 `startForeground` helper 传入 `FOREGROUND_SERVICE_TYPE_SPECIAL_USE`，低版本走兼容重载；manifest、权限和调用类型一致。该项只证明固定源码合同，不替代 Android 14+ 设备上的实际前台服务启动、通知可见性或类型拒绝测试 |
| Android bridge 日志与错误字段边界 | `android/common/.../GlobalState.kt`、`android/service/.../VpnService.kt`、`android/app/.../ServiceState.kt`、`ServiceController.kt`、`ServiceBroadcastReceiver.kt`、`plugins/ServicePlugin.kt`、`lib/common/http.dart`、`lib/common/request.dart`、`lib/providers/actions/setup.dart`、`lib/manager/connectivity_manager.dart`、`lib/views/proxies/common.dart` | 结构化、脱敏的诊断事件和稳定错误码 | Partial：固定 `GlobalState.log(text)` 直接调用 `Log.d("FlClash", text)`；`VpnService` 将 `route.address`/`prefixLength` 写入日志，core setup 的 message/exception 以及 service bind/start/stop/disconnect/unbind、broadcast action 的异常字符串也未经字段脱敏直接拼接日志。`ServicePlugin` 还将 `error.message` 原样作为 Flutter `CORE_ERROR`，而通用 `showToast` 接受任意调用方文本，故异常正文可能继续进入 MethodChannel/UI。Flutter `commonPrint` 还直接记录 HTTP `url`、更新异常、配置 `realLabel`、Wi‑Fi SSID、连通性结果和 IP 检查响应；延迟测试失败日志进一步拼接实际 `proxyName` 与异常对象。这些生产者没有统一字段脱敏层。静态源码不能证明任一运行时消息必含订阅 URL、凭据、Cookie、节点名或请求内容，本轮未读取 logcat、应用日志、配置、节点、订阅 URL、凭据、Cookie 或请求；XToolpro 必须使用稳定错误码、默认关闭敏感诊断并在日志/导出边界统一脱敏，完成契约测试前保持 `Partial` |

| 配置导出 `FilesProvider` 范围与 URI 授权 | `android/app/.../FilesProvider`、Android manifest、配置导出路径 | 最小化 SAF 导出与可撤销授权 | Partial：固定 Android `FilesProvider` 以 `MANAGE_DOCUMENTS` 保护，但把整个 app `filesDir` 暴露为根目录；canonical path 检查只阻止越出该根，普通文件仍可按请求模式打开且标记为可写。既有真机仅验证配置导出 SAF 选择器可达和文件名对应，未打开、读取或验证 URI grant 撤销；不能把 provider 可达性等同于最小范围、只读或导出完成。XToolpro 必须将配置导出限制在专用临时目录和最小只读 URI，采用一次性/可撤销 grant 与提交后清理，并以 `Success`、`Unavailable`、`Cancelled`、`EngineCrashed`、`VersionMismatch` terminal receipt 验证 |
| 应用更新 metadata 与 artifact 信任边界 | `lib/common/request.dart`、`lib/views/about.dart`、`lib/providers/actions/common.dart`、发布元数据入口 | 版本检查、签名 artifact、可回滚安装 | Partial：固定路径只请求 FlClash Release metadata 并比较 `packageInfo.version`；真机“检查更新”显示已是最新版，未进入下载或安装。静态补充：固定 `request.dart`（SHA-256 `8073CEFF3D34FF7F2487AA0BE2EC8309AD3A68B5CFB63C0C8404ADB634E09095`）调用固定 GitHub `releases/latest` URL，仅检查 HTTP 200、读取 `tag_name` 并用 `compareVersions` 判断更新；`about.dart`（`671166BF7057D4C3E7DC29DC86DF9E694EA9678443809683BEE05052CCE5CBCF`）和 `providers/actions/common.dart`（`9AA4F5391C40485EC1A869B5329A6D0979AE458C5389DBF298BA0ABB879EAD6B`）只展示 release body 摘要并打开固定 GitHub latest 页面，未选择或下载 release asset。未发现 artifact 签名/哈希、来源绑定、安装前 staging、ABI/版本兼容校验、失败回滚或 update terminal receipt；请求异常被压成空结果/通用提示，自动检查关闭偏好可被用户拒绝更新后持久化。不能把 metadata 检查或“前往下载”入口视为已验证更新能力。XToolpro 必须以受签名 manifest 校验版本、来源、hash、兼容性和回滚，并区分 `Success`、`Unavailable`、`Cancelled`、`EngineCrashed`、`VersionMismatch` |

| 代理组策略、健康选择与空成员回退 | `core/Clash.Meta/adapter/outboundgroup/{parser,groupbase,selector,urltest,fallback,loadbalance}.go`、`adapter/provider/healthcheck.go` | 组策略、健康状态和失败回退合同 | Partial：固定归档只注册 `select`、`url-test`、`fallback`、`load-balance`，`relay` 明确返回移除错误；空结果统一回落 `empty-fallback`（默认 `COMPATIBLE`）。`select` 成员不存在时回到首项；`url-test` 使用 10 秒单飞缓存、fixed selection 和 tolerance；`fallback` 优先健康成员，固定成员失效后清除并回退；`load-balance` 支持 consistent-hashing、round-robin、sticky-sessions，可用成员耗尽时回到首项且 `Now()` 为空。健康检查默认 5 秒 timeout、5 次失败触发、provider 单轮最多 10 并发并可 lazy 跳过。未形成空 provider、全失败、超时、选择失败或 core 同步失败的稳定 UI 状态和回滚回执，保持 `Partial`。 |

| 命名 inbound listener 重载与失败回滚 | `core/Clash.Meta/listener/{listener,parse}.go`、`core/Clash.Meta/config/config.go`、`core/Clash.Meta/hub/executor/executor.go` | 配置重载、监听器替换和失败回滚 | Partial：`parseListeners` 按列表逐项解析，缺失类型、未知协议、重复名称或任一项解码失败都会使整个 listener map 返回错误；`updateListeners` 再以 `PatchInboundListeners` 更新运行态。该函数在配置变化时先 `oldListener.Close()`，随后才调用新 listener 的 `Listen`；若新监听失败，函数仅记录错误并继续，已关闭的旧对象仍可能留在 `inboundListeners` map 中，没有原子替换、回滚、失败分类或 terminal receipt。删除仅在 `dropOld=true` 且新 map 不含名称时执行；Android bridge 对内置 HTTP/SOCKS/redir/tproxy/mixed 等端口另走重建函数，不能把命名 listener 重载语义外推为设备可用。XToolpro 需以 prepare/validate、原子切换、旧实例保留或明确 unavailable 结果及可核验健康回执覆盖重载失败、取消和重试，保持 `Partial`。 |

## sdmaid-se：设备维护能力

固定提交：`b9b01ee0af648fa6af25d388bb39bacde8d5b7a9`
引擎：`engine-cleaner`
来源记录：[ADR-0003](ADR/ADR-0003-sdmaid-cleaner-engine-investigation.md)、[依赖盘点](../compliance/upstream-dependency-inventory.md)

| 上游能力 | 来源路径/组件 | XToolpro 映射 | 状态 |
| --- | --- | --- | --- |
| 残留、缓存、系统缓存、缩略图、日志、临时文件 | `app-tool-corpsefinder`、`app-tool-systemcleaner` | 分类扫描、规则解释和候选预览 | Pending |
| 空目录、安装包、大文件 | `app-tool-systemcleaner`、`app-tool-analyzer` | 扫描结果、排序、定位和安全策略 | Pending |
| 卸载残留识别、排除和误报反馈 | `app-tool-corpsefinder`、`app-common-exclusion` | 规则详情、排除项和反馈状态 | Pending |
| 应用缓存和应用清理 | `app-tool-appcleaner`、`app-common-pkgs` | 按应用清理、批量操作和风险确认 | Pending |
| 系统清理和高风险项目 | `app-tool-systemcleaner`、`app-common-root` | 权限检测、二次确认和不可用原因 | Pending |
| 重复文件名称/大小/哈希分组 | `app-tool-deduplicator` | 分组、保留建议、逐项选择和预览 | Pending |
| 图片、视频、文档重复项分类 | `app-tool-deduplicator` | 类型筛选、缩略图和结果汇总 | Pending |
| 存储目录树、类型分布、最大/最近文件 | `app-tool-analyzer` | 存储分析图表、列表和定位 | Pending |
| 路径搜索、排序、最近文件和收藏保护 | `app-tool-analyzer`、common data | 搜索、排序、保护规则 | Pending |
| SAF 范围内浏览、重命名、移动、复制、分享 | `app-common-io`、`ViewIntentTool` | 文件操作事务和结果验证 | Partial（静态）：固定 `SAFGateway` 以持久化 URI grant 定位 scoped tree，可按快照枚举、lookup、walk、读取 `canRead/canWrite`，并在创建子项时核对 provider 返回的名字；`PathMapper` 对持久 grant 升级失败会尝试回退新增 flags。可是 `APathGateway`/`GatewaySwitch` 没有 rename、move 或 copy 合同，固定 SAF 代码也未调用 `DocumentsContract.renameDocument/moveDocument/copyDocument`。`ViewIntentTool` 仅为 `LocalPathLookup` 通过 `FileProvider` 创建 `ACTION_VIEW` chooser，附带读写 URI grant；它不是 SAF 分享/导出事务，也没有收回外授 URI 的契约。路径、URI、grant 和异常会进入日志。XToolpro 必须把浏览与 mutation 分开：每次 mutation 在同一 SAF scope 内完成预检、临时输出、内容/metadata read-back、原子发布和失败清理；分享只授予最小只读、最短生命周期 URI，并验证撤销。完成真实 provider 的 success/unavailable/cancel/crash/version-mismatch 与隐私测试前保持 `Partial` |
| SAF 删除、回收站、恢复、安全擦除 | `app-common-io`、`app-tool-deduplicator` | 预览、暂存、恢复和不可恢复提示 | Partial（静态）：固定 `SAFGateway.delete()` 先按持久化 URI permission 定位文档，保留 `CancellationException`，并在 provider 返回 `false` 时以 `existsStrict()` 核验；目录删除优先直接级联，失败后按后序逐项删除。重复项 UI 在提交 `DeduplicatorDeleteTask` 前会产生预览/确认事件，实际 deleter 逐项调用 `dupe.path.delete(gatewaySwitch)`。但 `SAFDocFile.delete()` 直接调用 `DocumentsContract.deleteDocument`，固定源码未发现由该删除链产生的回收站、内容恢复或安全擦除实现；批量删除没有逐项 `Success`/`Failed`/`Cancelled` 结果，单项异常可中止任务，而 task success 只含最终受影响集合。URI、路径和异常也会进入日志。XToolpro 必须在提交前锁定预览快照，采用可恢复暂存或明确不可恢复的系统/provider 语义，以逐项、可取消且脱敏的结果收敛 permission loss、部分成功、provider crash 与版本差异；完成真实 SAF 五类契约与恢复/不可恢复验证前保持 `Partial` |
| 已安装/系统应用列表与信息导出 | `app-tool-appcleaner`、`app-common-pkgs` | 应用信息页和显式导出 | Pending |
| 冻结、停用等高风险操作 | `app-tool-appcleaner`、Root/Shizuku 路径 | 权限门禁、二次确认和回滚状态 | Pending |
| Root、Shizuku、ADB 可选增强 | `app-common-root`、`app-common-adb` | 能力检测；无权限时不得伪造成功 | Pending |
| 定时扫描、Wi-Fi/充电、低电量策略 | 工具设置与 WorkManager 路径 | 后台计划、通知和跳过原因 | Pending |
| 通知摘要、执行历史、白名单/黑名单 | common data、设置路径 | 任务中心、策略导入导出和审计 | Pending |

## ytdlnis：媒体能力

固定提交：`13320bb64f35c8d04f01bebfa782d7947758fb66`
引擎：`engine-media`
来源记录：[ADR-0004](ADR/ADR-0004-ytdlnis-media-engine-investigation.md)、[媒体阶段规范](../specs/07-media-module.md)

| 上游能力 | 来源路径/组件 | XToolpro 映射 | 状态 |
| --- | --- | --- | --- |
| URL、批量 URL、系统分享、剪贴板识别 | `ShareActivity.kt`、`MainActivity.kt`、`HomeFragment.kt`、`UiUtil.kt`、manifest | 解析入口和批量校验 | Partial（静态）：固定 manifest 为 exported `ShareActivity` 声明 `ACTION_SEND text/plain` 与多个 HTTP(S) `ACTION_VIEW` filter；该 Activity 从 `EXTRA_TEXT`/data 取值并 `extractURL()`，可依偏好直接后台排队。主页 clipboard 检查会读取 primary clip，按换行筛出多个 `Patterns.WEB_URL`；文本文件分享也可被 `MainActivity` 全量读入并传到 URL 输入。入口没有逐 URL 用户确认、来源/权限/内容类型约束、规范化与 allowlist 结果、批量部分失败收敛或取消 receipt；`ShareActivity` 还 `Log.e` 整个 intent，URL 和异常/复制日志可进入系统日志或剪贴板。XToolpro 必须在显式用户动作后以脱敏的结构化输入解析与逐项确认处理共享/剪贴板，不得自动持久化或后台请求；真实输入、拒绝、取消、批量部分失败和隐私契约验证前保持 `Partial` |
| 播放列表/频道、部分选择、增量下载 | `YTDLPUtil.kt`、`ResultDao.kt`、`DownloadWorker.kt` | 队列拆分、跳过已下载和进度 | Partial（静态）：固定解析可保存 playlist title/URL/index，结果表支持按 playlist 过滤；单项构建时可向 yt-dlp 传 `--match-filter` 或 `-I`，并可选 `--download-archive` 跳过已下载。其没有不可变成员快照、页码/版本/etag、每成员授权或统一部分成功结果；archive 命中仅由“无 final paths”显示为已存在，无法区分跳过、空产出与失败。playlist URL、标题、成员 metadata、filter 和命令仍会进 Room/history/log。XToolpro 必须在显式用户选择后持久化可审计成员快照和逐项状态，稳定区分 skipped/success/failed/cancelled，默认脱敏敏感 source；真实 playlist/channel、增量、部分选择、并发变更和取消验证前保持 `Partial` |
| 标题、作者、封面、时长、描述、章节、元数据 | yt-dlp/NewPipe extractor | 真实详情页和任务快照 | Pending |
| 视频、音频、缩略图、字幕、元数据下载 | `YTDLPUtil.kt`、`DownloadWorker.kt`、yt-dlp/FFmpeg | 格式任务和输出验证 | Partial（静态）：固定解析读取 title、author、duration、thumbnail、chapters 和 available subtitles；请求构造可传 `--write-thumbnail`、`--write-description`、`--write-subs`/`--write-auto-subs`、`--embed-subs`、`--sub-format`、`--convert-subtitles`、`--sub-langs`、`--embed-thumbnail` 与 `--embed-metadata`。但 worker 仅由 stdout/迁移路径推导 `finalPaths`，随后明确排除缩略图、字幕、description 和 txt；history 仅对首个媒体文件读大小、扩展名和时长，执行成功后仍可能无媒体输出，且没有逐类输出 terminal result、文件/容器/metadata read-back、hash、post-process 原子性或部分成功 receipt。真实格式、输出、取消/失败清理与隐私契约验证前保持 `Partial` |
| 容器、编码、分辨率、帧率、音质、语言选择 | `YTDLPUtil.kt`、`FormatUtil.kt`、`DownloadViewModel.kt`、yt-dlp | 格式选择器和能力提示 | Partial（静态）：固定资源与选择逻辑声明音频/视频 container、约 2160p 至 240p 分辨率、约 192kbps 至 64kbps 音频、codec、DRC 和语言偏好；请求会组合 `-f`、`-S`、`--audio-format` 与音轨 language filter，并以 best/worst fallback 继续。它不验证每个 source/版本实际提供的 format、codec/container 兼容性、fps/分辨率/码率/语言是否与选择一致、设备解码性或 fallback 原因；worker 也没有逐输出 format read-back/receipt。真实可用格式、unsupported/fallback、音视频合并与设备兼容性验证前保持 `Partial` |
| 音视频合并、音频提取、转码 | `YTDLPUtil.kt`、yt-dlp/FFmpeg | 后处理任务、取消和恢复 | Partial（静态）：固定请求可使用 `-x`、`--audio-format`、`--audio-quality`、`--merge-output-format`、`--recode-video` 与 FFmpeg postprocessor；兼容模式会强制 mp4/h264/aac，裁剪/移除音轨会加 `FFmpegCopyStream` 参数。没有 post-process 输入/输出映射、临时提交、完整性/解码 read-back、逐步骤 cancel/crash/rollback 或失败/部分成功 receipt；实际 runtime/ABI 和设备兼容性也未验证。真实合并、提取、转码及恢复验证前保持 `Partial` |
| 嵌入封面/字幕、元数据写入、时间裁剪 | FFmpeg/yt-dlp | 后处理选项和结果验证 | Pending |
| Cookie 文件、浏览器 Cookie、登录会话 | `app/` session flow | 加密本地存储、撤销、删除和授权提示 | Partial（静态）：固定 ytdlnis `CookieItem` Room entity 直接保存 `url`、`content`、`description` 与 enabled 标记；`DBManager` 使用普通 `Room.databaseBuilder`，对固定源码/Gradle 的针对性检索未见 SQLCipher、EncryptedSharedPreferences 或 Android Keystore 的 Cookie 数据库保护。`CookieViewModel.updateCookiesFile()` 将启用项组合为 Netscape 格式并写入 `context.cacheDir/cookies.txt`，解析/下载与终端路径在 `use_cookies` 为真时把该文件路径传给 yt-dlp `--cookies`。同一 ViewModel 可将全文读入系统剪贴板或复制到导出目录；设置备份序列化全部 Cookie entity 为 JSON。删除全部 Cookie 会删除 Room 行并把当前缓存文件写空，但固定代码没有涵盖已复制到剪贴板、导出或备份文件的撤销/安全擦除。该审计未导入、读取或使用真实 Cookie/session，不能据此断言特定设备上的文件权限或泄露。XToolpro 必须以 Keystore 绑定的加密 vault 保存会话，仅在短生命周期、最小权限内部文件中生成受控 yt-dlp 输入；默认禁止原始 Cookie 的日志、剪贴板和通用备份/导出，并将授权、删除、撤销及遗留文件清理纳入有界、可验证 contract。在真机授权 session、加密/删除/恢复和隐私审查完成前保持 `Partial` |
| 用户授权的私有、付费和高级格式 | yt-dlp authenticated path | 会话任务；仅限用户有权访问内容 | Pending |
| 并发、暂停、恢复、取消、重试、断点 | `DownloadWorker.kt`、`DownloadRepository.kt`、`DownloadViewModel.kt`、`RuntimeManager.kt`、notification receivers、yt-dlp request | 持久任务状态机和通知 | Partial（静态）：固定 ytdlnis 有 Room `Queued/Active/Paused/Error/Cancelled` 状态、前台 `DownloadWorker`、`concurrent_downloads` 上限及延迟时的单任务 mutex；每次 start 以时间戳为唯一 work 名、`ExistingWorkPolicy.REPLACE` 入队，暂停/恢复/取消会分别取消 tagged work、终止内存 process map 中的子进程并改写 Room 状态。请求可传入 yt-dlp `--retries`、`--fragment-retries`，默认可保留 `.part`，但 cache 输出目录在任务开始和失败时均直接删除；Worker 对单个下载失败最终仍返回 `Result.success()`，并且 worker/receiver 多处吞异常或仅以 1 秒延时改写状态。进程 map 不持久化，进程死亡后不能据此归因；取消/暂停没有持久 completion receipt，恢复没有输出 read-back、文件完整性或部分成功收敛，且 error/log/notification 路径可携带原始异常或命令/URL。XToolpro 必须采用可持久化的任务状态机和每项 terminal receipt，以稳定脱敏结果区分 success、unavailable、cancel、crash、version mismatch；真实断点、并发、恢复、取消、重试及输出完整性验证完成前保持 `Partial` |
| 命名/路径模板、冲突策略、按列表分目录 | `FolderSettingsModule.kt`、`YTDLPUtil.kt`、`FileUtil.kt`、`MoveCacheFilesWorker.kt` | 模板预览、SAF 输出和原子提交 | Partial（静态）：固定设置以 `ACTION_OPEN_DOCUMENT_TREE` 取得读写 persistable tree grant，并保存音频/视频/命令/缓存路径；yt-dlp 接受音视频文件名模板和可选 website/playlist 等子目录。直接文件系统目标发生同名时以递增编号改名，tree move 的 library callback 也请求 `CREATE_NEW`；但 cache 输出迁移有 `REPLACE_EXISTING` 分支。SAF fallback 先 `DocumentsContract.createDocument` 再流拷贝，随即删除源；无临时同目录 staging、fsync、长度/hash/metadata read-back、原子发布或跨 provider rollback。逐文件异常被记录/吞掉，仍返回累积路径并扫描媒体；缓存迁移完成后递归删除原目录，未建模部分成功、取消或崩溃恢复。路径、异常与 FileProvider/分享路径也可进入日志。XToolpro 必须在 scoped SAF grant 内实施预检、临时输出、read-back、原子提交及逐项脱敏 terminal receipt；真实 provider、冲突、permission loss、取消、崩溃和版本错配验证前保持 `Partial` |
| 历史、取消记录、备份恢复 | `HistoryRepository.kt`、`DownloadDao.kt`、`SettingsViewModel.kt`、`BackupSettingsUtil.kt` | 任务历史和可恢复快照 | Partial（静态）：固定 Room history/cancelled/error/saved/queued/scheduled 表路径支持分页、状态筛选和 JSON 备份；成功任务会写 history，取消项可列出或再入队。backup 将 history、任务、Cookie、偏好、模板等序列化为未加密 JSON 并直接写入公共备份目录；history 含 URL、标题、作者、缩略图、输出路径、format 与 command。restore 先清空 preferences/各目标表再逐条插入，跨偏好和多张表无事务、无 staging、schema/version/完整性/来源校验、冲突预览或 rollback；固定 `restoreData()` 甚至没有处理已导出的 `scheduled` 集合。历史删除可先删 Room 行再尝试删文件，失败被吞掉；清理 worker 也会删除 cancelled/error 与 cache。XToolpro 必须加密敏感快照、最小化备份内容、校验版本/来源，采用预览、原子/可回滚 restore 和逐项脱敏结果；静态补充：`_restoreTask()` 将 ZIP entry 经 `posix.normalize(file.name)` 直接拼接到 restore 目录后写出，未见 canonical-path containment 或拒绝绝对/父级路径；随后 `_copyWithMapList()` 以普通 `File.copy()` 并发迁移 profile/script，仍无逐项校验、原子发布、失败清理或回滚。该路径逃逸风险仅由源码推断，未构造恶意归档或访问设备文件。真实导出、损坏/错配、取消、部分失败和恢复验证前保持 `Partial` |
| 后台通知、完成动作、开机恢复 | `DownloadWorker.kt`、`NotificationUtil.kt`、`AlarmScheduler.kt`、`ScheduleAlarmReceiver.kt`、`ObserveBootReceiver.kt`、manifest | 后台任务和恢复策略 | Partial（静态）：固定下载 Worker 提供前台 WorkManager notification，运行通知可暂停/取消；完成/失败通知含打开、分享、重新配置和日志入口。下载排程可通过 exact alarm 入队，并有 network constraint；但完成通知会放入标题、输出路径，错误通知直接放入原始 error，均为公开可见通知，且可授外部分享 URI。manifest 的 BOOT_COMPLETED/MY_PACKAGE_REPLACED receiver 仅重建 observe-source alarm，不恢复下载任务/排程或校验未完成状态；下载 alarm cancel 用 `getService` 查找、创建端却为 broadcast，不能构成可靠取消确认。Work/receiver 异常和通知权限/通道关闭没有统一持久状态或 terminal receipt。XToolpro 必须用最小化、脱敏通知，显式最小读 URI 授权，并以持久化、可核验的开机恢复/排程/取消状态机覆盖 unavailable、cancel、crash、version mismatch；真实通知权限、reboot、排程和恢复验证前保持 `Partial` |
| yt-dlp、FFmpeg、Aria2c、Python/JS runtime 管理 | `core/RuntimeManager.kt`、`YTDLUpdater.kt`、`core/packages` | 版本、来源、校验、更新和回滚 | Partial（静态）：固定构建声明 youtubedl-android library/Aria2c `0.18.1`、FFmpeg `0.17.2`；`RuntimeManager` 管理 Python、FFmpeg、Aria2c、NodeJS、Deno、QuickJS 与 APK resource 初始化的 yt-dlp，并用 update latch 阻止命令与更新并发。yt-dlp updater 从 release API 取得 tag/asset URL，下载到 cache 临时文件后却先删除现有运行目录再复制；固定路径没有 SHA-256、签名、资产长度、许可证/NOTICE/SBOM 或兼容性验证，也不保留上一已知可用版本。复制失败只删除新目录并重新初始化内置 resource，不能作为版本化 rollback；版本仅写入 SharedPreferences，错误会带原始 exception。2026-09-08 受限真机前置确认设备为 arm64-v8a/API 33，但已知 ytdlnis/dev 包未运行、主机无可执行 runtime；对隔离 clone 的固定提交执行 `git ls-tree -r --name-only 13320bb64f35c8d04f01bebfa782d7947758fb66`，确认顶层无 `library/` 或 `ffmpeg/`，而祖先 `0ef8f5bf` 曾包含两模块，`2b28f347` 的历史变更将其删除。固定提交的 `settings.gradle` 仍 include `:common`, `:app`, `:library`, `:ffmpeg`，`app/build.gradle` 仍声明外部 youtubedl-android 坐标；因此源码归档闭包不可由该提交单独重建，不能把未知设备包、host 工具或临时替换模块当作固定提交 proof。XToolpro 必须为每一实际 ABI/runtime 建立签名或 SHA-256 锁定 manifest、许可证和兼容矩阵，先校验 staging artifact 再原子切换，保留可验证 last-known-good rollback，并以脱敏 contract 覆盖 unavailable、cancel、crash 与 version mismatch；完成真实更新/回滚五类验证前保持 `Partial` |
| 终端和自定义命令 | Termux components、command path | 明确授权、沙箱边界和脱敏输出 | Partial（静态）：固定 ytdlnis `MkSession` 创建交互式 shell，会生成带 Python、FFmpeg、Node、Deno、Aria2c 与 yt-dlp 可执行路径的 shell functions；`use_cookies` 时该 yt-dlp function 同时拼接内部 Cookie 文件的 `--cookies` 参数。`TerminalFragment` 直接将选中的 command-template 或 shortcut 文本写入会话；`CommandTemplate` 内容可持久化、剪贴板导入/导出，缺少命令 allowlist、参数结构化或逐次风险确认。`TerminalItem` 与 DAO 又把完整 command 和 terminal log 放入普通 Room 表；固定路径未见针对 URL、Cookie、令牌或输出的统一脱敏。该审计不执行命令、不读取真实终端内容、Cookie、URL、下载、日志或设备文件，不能断言某一命令实际可执行或曾泄露数据。XToolpro 仅能在单独受限环境中，以逐次明确授权、结构化允许参数、最小 SAF scope、可取消进程和默认脱敏输出提供必要补充；不得将上游通用终端或未审计模板直接暴露给产品流程。完成隔离、取消、敏感输出和滥用审查前保持 `Partial` |

## ImageToolbox：图片能力

固定提交：`cb73d7a2e3094fb49e4d32cb07ad2903b62f8ac0`
引擎：`engine-image`
来源记录：[ADR-0005](ADR/ADR-0005-imagetoolbox-image-engine-investigation.md)、[图片阶段规范](../specs/08-image-module.md)

| 上游能力 | 来源路径/组件 | XToolpro 映射 | 状态 |
| --- | --- | --- | --- |
| 裁剪、旋转、翻转、透视、画布扩展、缩放 | `feature:crop`、`lib:cropper`、`lib:image` | 非破坏编辑会话和预览 | Pending |
| 撤销/重做、比较、实时预览 | `core:domain`、`feature:single-edit`、`feature:compare` | 编辑状态、历史和差异预览 | Pending |
| 亮度、对比度、饱和度、色相、伽马、曝光、温度、色阶、曲线 | `core:filters`、`lib:curves` | 参数面板和确定性导出 | Pending |
| 锐化、模糊、噪点、像素化和滤镜 | `core:filters`、`lib:*` filters | 滤镜列表、预览和批处理 | Pending |
| 画笔、形状、箭头、文字、贴纸、马赛克、边框、圆角、阴影、背景、图层 | `feature:draw`、编辑 feature paths | 图层编辑器和导出 | Pending |
| JPEG/PNG/WebP/HEIF 及其他支持格式转换 | `feature:format-conversion`、image codecs | 格式、质量、色彩空间和 DPI | Pending |
| 目标大小/质量压缩和尺寸控制 | `feature:resize-convert`、`core:data` | 压缩预览、估算和结果验证 | Pending |
| 批量裁剪、缩放、重命名、转换、压缩、水印、滤镜 | batch/tool feature paths | 批量任务、单项重试和汇总 | Pending |
| 拼图、长图、网格、图片叠加 | collage/composition feature paths | 布局编辑和输出 | Pending |
| 前景/背景替换、擦除背景 | `feature:erase-background`、`lib:neural-tools`、`core:data` | 模型能力检测、离线处理和结果确认 | Partial（静态）：固定 ImageToolbox `BgRemover` 覆盖 `RMBG1_4`、InSPyReNet、U2NetP/U2Net、BiRefNet、MODNet、ISNet 和 YOLO；U2NetP 从随 APK 的 `assets/u2netp.onnx` 提取，其余模型通过可注入 downloader 从固定 `HF_BASE_URL` 指向的 Hugging Face 路径下载到内部 `filesDir/ai_models`。通用下载器使用临时同级文件和原子 rename，失败/取消后删除临时文件；但每个背景模型的 `checkModel()` 仅把“文件存在且长度大于零”作为已下载，未绑定模型 ID、版本、预期长度或 SHA-256，随后会直接创建 ONNX session。固定源码没有这组模型的逐项许可证/provenance 清单，也没有把下载、校验、ONNX 加载或推理异常映射为稳定的 `Unavailable`/`Cancelled`/`EngineCrashed`/`VersionMismatch`；UI 路径会将 `Throwable` 交给通用 failure toast。XToolpro 必须在允许下载或加载前锁定每个模型的来源、许可证、版本、大小与 SHA-256，下载到临时文件后校验再原子发布，并以可取消、脱敏且有界的 contract 分离 unavailable、cancel、load/crash、version mismatch；完成真实模型及五类契约测试前保持 `Partial` |
| EXIF 查看/编辑/清除、颜色/尺寸信息、哈希 | `core:data`、`feature:edit-exif`、`feature:delete-exif`、`feature:checksum-tools` | 元数据策略、信息面板和校验 | Partial（静态）：固定路径支持读取/编辑、预设删除标签、清空 metadata、保留日期和 checksum；删除 EXIF 以新目标或 cache 副本保存，并将 `keepOriginalMetadata=false` 交给 file controller，后者会清除全属性或仅复制所选 metadata。该源码也含 metadata round-trip instrumentation tests，但本轮未构建或运行，故不能视为不同格式/SAF 输出的实证。更重要的是 `AndroidFileController` 在 save/move/read/copy metadata 路径把 URI、完整 initial/source/destination metadata 和异常交给 `makeLog`，而 OCR 可把识别文本写入 `UserComment` 后再进入这些 metadata 操作；固定路径未见统一敏感字段脱敏、输出 read-back 校验或对 cache/share 副本的有界清理合同。XToolpro 必须默认隐藏并禁止记录位置、设备标识、注释和 OCR 文本等敏感 metadata，采用临时输出、原子提交与格式化 read-back 验证，并将 SAF 不可写、cancel、codec/metadata crash、格式/版本不匹配映射为稳定结果；完成真实设备及五类契约前保持 `Partial` |
| 取色器、调色板、曲线和颜色比较 | `feature:pick-color`、`lib:curves` | 取色、调色和编辑参数 | Pending |
| 图片转 PDF、PDF 页面转图片 | PDF/tool feature paths | 文件转换任务和输出验证 | Pending |
| GIF 帧提取、SVG、二维码/条形码 | format/archive/QR paths | 导入、编辑和导出 | Pending |
| OCR/文字提取和模型能力 | `feature:recognize-text`、`lib:neural-tools`、Tesseract4Android | 本地模型、隐私提示和 unavailable 状态 | Partial（静态）：固定 feature 提供 Tesseract、PaddleOCRv5/v6；Paddle 的 13 个语言/脚本 ZIP 由 Hugging Face `main` 分支下载，Tesseract best/standard/fast `.traineddata` 直接来自各 `tessdata*` 仓库 `main` 分支。两条路径只检查必需文件是否存在（Paddle 为文件名组合，Tesseract 为单文件存在），未锁定 commit、版本、长度或 SHA-256，也没有逐模型许可证/provenance manifest。Paddle ZIP 解包前未验证 hash；Tesseract 语言包可导出，导入时直接以 zip entry name 写入 internal `filesDir/tesseract`，固定代码未作 canonical-path containment 检查。识别异常虽可形成内部 `NoData`/`NoPaddleData`/`Error`，但 `Error` 携带原始 throwable 且 UI 可将其写入批量输出或 failure toast，不能作为脱敏、稳定的 engine contract。XToolpro 必须锁定各训练数据/ONNX bundle 的来源、许可、hash 和解析边界，拒绝路径逃逸/损坏包，并将缺失、取消、加载崩溃、版本不匹配映射为有界脱敏结果；完成真实模型五类契约和隐私输出审查前保持 `Partial` |
| OpenCV、GPU、native codecs、AI models | `lib:opencv-tools`、`lib:neural-tools`、ImageToolboxLibs、`app/build.gradle.kts` | ABI、内存、版本、校验和与回滚 | Partial（静态）：固定 version catalog 锁定 OpenCV `5.0.0.1`、ONNX Runtime Android `1.29.0`、`aire` `0.18.1` 与 ImageToolboxLibs `8.0.6`；后者提供 GPUImage 及 GIF/APNG/JP2/QOI/WebP/PSD/DjVu/RAW/TIFF/GMIC 等多个 native/codec 组件。app 只声明 `armeabi-v7a`、`arm64-v8a`、`x86_64`，对非 bundle 构建开启 ABI split/universal APK，并以 `pickFirst` 处理至少 `libcoder.so`；market flavor 另引入 ML Kit subject/selfie segmentation，foss flavor 不引入该路径。固定源码归档没有自产 `jniLibs`，本轮也未解析/构建第三方 AAR 或 APK，故尚无逐 ABI 实际库清单、hash、符号/加载结果、内存/设备兼容性、许可证/NOTICE/SBOM、GPU fallback 或 rollback 证据。XToolpro 必须对每个实际交付 ABI 和 flavor 锁定所有 native/AI artifact 的来源、许可证、hash 与加载契约，并真实验证 GPU/CPU fallback、unsupported ABI、cancel、native crash 与 version mismatch；完成前保持 `Partial` |

## 批准门禁

1. 每一行都必须绑定固定提交、实际源码/组件路径、依赖树、许可证和测试证据。
2. `Pending` 不能在产品文案或 UI 中宣称已集成；`Unavailable` 必须有真实设备/系统原因；`Blocked` 必须有 ADR 和回滚路径。
3. 只有四个域的矩阵均完成，且 success、unavailable、cancel、crash、version mismatch 契约测试通过，台账行才允许进入 `Approved`。
4. 上游更新后重新计算受影响行；不能用新版本行为替代锁定提交的历史证据。
