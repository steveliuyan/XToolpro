# Upstream Reuse Ledger

This ledger is required before integrating an upstream component. One row may cover a coherent capability set only when its code path, license, and update policy are the same. Replace `TBD` with evidence during Phase 02.

| Domain | Requirement IDs | Upstream repository | Locked commit/tag | Reuse mode | Upstream path/component | License status | Android compatibility | Contract tests | Owner | Update/rollback policy | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Proxy | `PRO-*` | `chen08209/FlClash` + `chen08209/Clash.Meta` | FlClash `62addf738a76b1a492e19af2dbabdb6d572b9e72`; Clash.Meta gitlink `0f7f05adff5e2c49775a112dcfe05a6aa36fda0c` | Investigating: full-capability fork-port behind `engine-proxy` | Complete proxy/core, profile/config, groups/nodes, routing, VPN bridge, diagnostics, update and backup paths under `core/`, `services/helper/`, `android/`, and `setup.dart`; `core/go.mod` binds mihomo to the locked Clash.Meta work tree; replace only Flutter UI/assets with XToolpro shell | FlClash and locked Clash.Meta GPL-3.0 confirmed; Android proof excludes the three `TODO`-licensed desktop plugins, but their licenses remain unresolved for desktop/full-capability routes; other third-party core review required | Android build requires Flutter, Go, Android SDK/NDK, the locked submodule, and git-pinned `flutter_distributor`; package step enables ABI splitting; dependency-crop checks must keep `proxy`/`rust_api`/`window_ext` out of the Android engine | Capability-parity matrix plus start/stop, unavailable core, cancellation, core crash, pinned-version mismatch | steveliuyan | Fork under `steveliuyan`; weekly upstream security/release review; native core and Android bridge deploy/rollback as a verified pair, with each ABI hash pinned | Investigating |
| Cleaner | `CLN-*` | `d4rken-org/sdmaid-se` | `b9b01ee0af648fa6af25d388bb39bacde8d5b7a9` | Investigating: full-capability fork-port behind `engine-cleaner` | All supported `app-tool-*` maintenance paths (`corpsefinder`, `appcleaner`, `systemcleaner`, `analyzer`, `deduplicator`) and their complete `app-common-*` closure; replace only excluded branding/assets/translations with XToolpro equivalents | GPL-3.0; upstream excludes icons, logos, marketing assets, documentation, and translations | Native Android Gradle multi-module project; full tool/common closure, SAF behavior, and optional Root/Shizuku paths require proof | Capability-parity matrix plus scan, duplicate analysis, cleanup preview/execution, recovery, unavailable SAF/root scope, cancel, scanner crash, pinned-version mismatch | steveliuyan | Fork under `steveliuyan`; monthly upstream/security review; patch series and rollback to locked commit; preserve complete supported tool surface | Investigating |
| Media | `MED-*` | `deniscerri/ytdlnis` + yt-dlp Android runtime | `13320bb64f35c8d04f01bebfa782d7947758fb66` (`youtubedl-android` 0.18.1; FFmpeg 0.17.2; Aria2c 0.18.1) | Investigating: full-capability fork-port behind `engine-media` | `app/` media parser and workflow, backed by yt-dlp execution; Cookie/session flow, format/playlist selection, queue/history/backup, command and component-management paths; do not reuse UI, assets, or name | GPL-3.0; upstream prohibits using `YTDLnis` name for downloader derivatives; yt-dlp/plugin licenses pending full inventory; user-authorized Cookie/private/premium flows are in scope, DRM/access-control bypass is not | Native Android Gradle project; upstream build declares min SDK 24/target SDK 36 and multiple ABI splits, compatible with XToolpro min SDK 26 pending proof | Public and authorized-session parse/download, unavailable engine, cancellation, engine crash, pinned-version mismatch, capability-parity matrix | steveliuyan | Fork under `steveliuyan`; weekly upstream/security review; binary/plugin checksums and rollback to locked commit; preserve yt-dlp and upstream capability behavior across updates | Investigating |
| Image | `IMG-*` | `T8RIN/ImageToolbox` | `cb73d7a2e3094fb49e4d32cb07ad2903b62f8ac0` | Investigating: full-capability fork-port behind `engine-image` | Complete supported processing closure across `core/`, `lib/`, and non-UI `feature/` paths, including editing, filters, codecs, batch, PDF/GIF/SVG/QR, OCR/model-backed tools, and metadata; replace only navigation/UI/brand assets | Apache-2.0 for repository code; retain NOTICE and inventory native/AI model dependencies | Native Kotlin/Compose multi-module project; upstream declares min SDK 24, compatible with XToolpro min SDK 26 pending proof; every supported ABI/codec/model path requires review | Capability-parity matrix plus transform success, unsupported input, cancellation, native crash, pinned-version mismatch | steveliuyan | Fork under `steveliuyan`; monthly upstream/security review; patch series and rollback to locked commit; preserve complete supported processing surface | Investigating |

## Allowed Reuse Modes

`official-module`, `fork-port`, `engine-adapter`, `isolated-process`, and `minimal-supplement`. `minimal-supplement` requires a linked ADR that explains why the first four modes cannot be used.

## Status Rules

- `Planned`: no source or license decision made.
- `Investigating`: source and build evidence is being collected.
- `Approved`: commit, license, integration mode, contract, and update plan approved.
- `Integrated`: contract tests pass in XToolpro.
- `Blocked`: an ADR explains the blocking condition and fallback.
